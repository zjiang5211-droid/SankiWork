---
title: Open Design 0.14.1
description: 可选择允许 Open Design 在下次启动时静默应用已下载的更新。
---

## 静默更新

现在可以允许 Open Design 在应用下次启动时静默应用已下载的更新。你可以在安装更新时启用该选项，也可以稍后在设置中管理。

只有明确点击安装后，这项偏好才会开启。它仅作用于启动时的自动更新检查，定期检查与手动安装流程仍保持原有行为。
