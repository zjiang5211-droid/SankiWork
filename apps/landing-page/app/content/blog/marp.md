---
title: "Marp: The Markdown Presentation Tool (and How to Export to PowerPoint)"
date: 2026-07-10
category: "Tools & Skills"
readingTime: 7
summary: "Marp is the open-source Markdown presentation ecosystem — write slides in Markdown, then use the Marp CLI to export HTML, PDF, or PowerPoint. This guide covers Marp themes, Markdown authoring, marp-cli export to pptx (as images), how it compares to reveal.js and Slidev, and where an agent-native design workspace fits."
noindexLocaleVariants: true
noindex: true
ctaKind: download-app
author: nadia-haddad
i18n:
  zh:
    title: 'Marp：Markdown 演示文稿工具（以及如何导出到 PowerPoint）'
    summary: 'Marp 是开源的 Markdown 演示文稿生态——用 Markdown 编写幻灯片，再通过 Marp CLI 导出 HTML、PDF 或 PowerPoint。本指南涵盖 Marp 主题、Markdown 编写、用 marp-cli 导出为 pptx（以图片形式），它与 reveal.js 和 Slidev 的对比，以及 Agent-Native Design Workspace 的适用场景。'
    category: '工具与技能'
    bodyHtml: |
      <p>如果你搜索"marp"是想找一个牙科或正畸术语，那这是另一个：<strong>Marp 是 Markdown 演示文稿生态（Markdown Presentation Ecosystem）</strong>——一个来自 GitHub 上 <code>marp-team</code> 的开源（MIT）项目，通过编写纯 Markdown 来构建幻灯片。不用在画布上拖拽方框，也没有私有文件格式。你写下 <code># Slide title</code> 和几个要点，加上一两条指令，Marp 就把它变成一份演示文稿。（你可能见过的、与此无关的"MARPE"是正畸学里的一种上颌扩弓手术——跟幻灯片毫无关系。本文只讲 Marp 这个演示文稿工具。）</p>
      <p>在开源演示文稿工具里，Marp 是最简单、最贴近 Markdown 原生的选择，这正是开发者青睐它的原因。这是一份诚实的指南，讲清楚 Marp 是什么、编写与主题如何运作、Marp CLI 如何导出到 PowerPoint 和 PDF（以及那个总让人踩坑的注意事项）、它与 reveal.js 和 Slidev 的对比，以及当 Markdown 走到尽头时，设计工作空间该如何接手。</p>
      <h2>Marp 是什么</h2>
      <p>Marp 是围绕一个理念构建的一小套工具：<strong>幻灯片即 Markdown</strong>。它由三个部分组成。<strong>Marpit</strong> 是底层框架，把 Markdown 加上主题转换成幻灯片的 HTML 和 CSS。<strong>Marp Core</strong> 是构建在 Marpit 之上、开箱即用的引擎，自带默认主题、图片处理和数学公式支持。<strong>Marp CLI</strong>（<code>marp-team/marp-cli</code>）是命令行工具，把你的 Markdown 文件渲染成 HTML、PDF 或 PowerPoint。还有一个官方的 <strong>VS Code 扩展</strong>，能在你输入时提供实时的并排预览。</p>
      <p>你在一个 <code>.md</code> 文件里编写。幻灯片之间用 <code>---</code> 分隔，逐页和全局设置通过 Marp 指令（如 <code>theme:</code>、<code>paginate:</code> 或 <code>class:</code>）来设定，一切都保持为可版本控制的纯文本。这正是它的全部魅力：你的幻灯片在 Git 里能干净地 diff、跟你的代码放在一起，也永远不会把你锁进某种二进制格式。</p>
      <h2>Marp 主题与 Markdown 编写</h2>
      <p>编写一份 Marp 幻灯片刻意保持极简。文件顶部的 front-matter 块设定全局指令，<code>---</code> 分隔幻灯片，而标准的 Markdown——标题、列表、代码块、图片、表格——就成了你的内容。因为 Marp 底层渲染成 HTML 和 CSS，任何 Markdown 能表达的东西都能映射到一张幻灯片，无需再学额外的语法。</p>
      <p><strong>Marp 主题</strong>就是 CSS。Marp Core 自带几个内置主题（<code>default</code>、<code>gaia</code>、<code>uncover</code>），你用一条 <code>theme:</code> 指令就能在它们之间切换。当内置主题不够用时，你把自己的主题写成一个 CSS 文件——针对 Marpit 暴露的类，通过 <code>--theme</code> 或 <code>--theme-set</code> 用 CLI 注册它，每一张幻灯片都会套用它。想做行内微调，你可以往某一张幻灯片里放一个带作用域的 <code>&lt;style&gt;</code> 块。这种 CSS 原生的模型既是 Marp 的天花板也是地板：如果你熟悉 CSS，就拥有完全的掌控力；如果不熟，默认样式看起来也很干净。</p>
      <h2>把 Marp 导出到 PowerPoint 和 PDF</h2>
      <p>导出正是 Marp CLI 体现价值的地方。把它指向你的 Markdown，选一个目标格式：</p>
      <ul><li><strong>HTML</strong>——<code>marp deck.md -o deck.html</code> 会生成一份自包含的网页幻灯片，供你在浏览器里演示。</li><li><strong>PDF</strong>——<code>marp deck.md --pdf</code> 渲染出一份可打印的 PDF，非常适合作讲义和分享。</li><li><strong>PowerPoint</strong>——<code>marp deck.md --pptx</code> 导出一个 <code>.pptx</code> 文件，你可以在 PowerPoint 或 Keynote 里打开。</li></ul>
      <p>有一个值得直说的注意事项：<strong>Marp 的 PPTX 导出会把每一张幻灯片渲染成一张图片</strong>，而不是原生、可编辑的 PowerPoint 形状。每张幻灯片都变成 <code>.pptx</code> 容器里一张满版的图。当你需要把文件交给一个只需演示、离不开 PowerPoint 的人时，这堪称完美——但导出之后，你没法点进去编辑文本框、给形状换色，或挪动某个要点。如果你的工作流之后还要在 PowerPoint 里继续编辑，就把 Marp 的 pptx 当成交付格式，而不是源格式。（实验性的"可编辑"导出模式来了又走；可靠且有文档记载的行为是一页一图。）</p>
      <h2>Marp 对比 Slidev 与 reveal.js</h2>
      <p>Marp 并不是唯一以 Markdown 为先的演示文稿工具。以下是围绕决定你选择的那些维度做的诚实对比：</p>
      <table><thead><tr><th>工具</th><th>编写方式</th><th>可编辑的 PPTX 导出</th><th>模板化</th><th>许可证</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>纯 Markdown + 指令</td><td>pptx 为图片（非可编辑形状）</td><td>CSS 主题</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML（可选 Markdown 插件）</td><td>无原生 pptx</td><td>HTML/CSS 主题</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + Vue 组件</td><td>无原生 pptx（PDF/PNG/SPA）</td><td>Vue + Windi/UnoCSS 主题</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>把它描述给你的 Agent</td><td>对着设计系统生成可编辑幻灯片</td><td>设计系统 + 模板库</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> 适合你想要最少繁文缛节的时候——一个 Markdown 文件加一个 CLI。<strong><a href="/blog/reveal-js/">reveal.js</a></strong> 是成熟的网页幻灯片框架：以 HTML 为先、可无限定制、拥有最丰富的浏览器内体验，但你写的是 HTML，而且没有原生的 PowerPoint 路径。<strong><a href="/blog/slidev/">Slidev</a></strong> 居于中间，适合想要 Markdown 加上真正的 Vue 组件、代码高亮和交互式嵌入的开发者——比 Marp 更强大，配置也更多。这三者都采用 MIT 许可证，且都无法直接导出可在 PowerPoint 里编辑的内容。</p>
      <h2>Open Design 的位置</h2>
      <p>Marp 在一件事上做得极好：把你已经写好的 Markdown 变成幻灯片。但它要求<em>你</em>来写这份幻灯片——结构、措辞、主题 CSS。它不会生成内容、不会让内容跨项目符合品牌调性，也不会在导出后给你一份可编辑的成果。</p>
      <p><a href="/">Open Design</a> 是它之上的那一层。它是一个开源（Apache-2.0）、本地优先、自备 key 的 <strong>Agent-Native Design Workspace</strong>，位于你已经在用的编程 Agent 之外。你不必手写 Markdown，而是描述你想要的幻灯片，Agent 就对着一套设计系统生成一份可编辑的——它的<a href="/plugins/templates/">插件库</a>里还提供了第一方的 HTML 幻灯片模板作为起点。它并不是要取代用 Markdown 编写的 Marp：当你已经把内容写进 Markdown、想从命令行快速得到一份幻灯片时，Marp 就是对的工具。当你想让幻灯片被生成出来、符合品牌调性，并且作为一整套更大的设计工作流的一部分仍然可编辑时，那就是工作空间的活了。如果你已经在用编程 Agent 做幻灯片，看看<a href="/blog/claude-ppt-skills/">编程 Agent 如何做幻灯片</a>。</p>
      <h2>常见问题</h2>
      <p><strong>什么是 Marp？</strong>Marp 是来自 GitHub 上 <code>marp-team</code> 的开源 Markdown 演示文稿生态。你用 Markdown 编写幻灯片，Marp CLI 把它们导出成 HTML、PDF 或 PowerPoint。它采用 MIT 许可证，包含 Marp Core、Marpit 框架和一个 VS Code 扩展。它与正畸学里的"MARPE"一词无关。</p>
      <p><strong>我怎么把一份 Marp 幻灯片导出到 PowerPoint？</strong>安装 Marp CLI（<code>marp-cli</code>）并运行 <code>marp deck.md --pptx</code>。注意导出的 <code>.pptx</code> 会把每张幻灯片渲染成图片，而不是原生可编辑的 PowerPoint 形状——它很适合交接，但导出之后你没法在 PowerPoint 里编辑文本或形状。</p>
      <p><strong>Marp 能导出可编辑的 PowerPoint 文件吗？</strong>不太可靠。Marp 有文档记载的 PPTX 行为是一页一图。如果你需要带真实文本框和形状、原生可编辑的 <code>.pptx</code>，那这一步 Marp 就是错的工具。</p>
      <p><strong>Marp 免费吗？</strong>免费。Marp 在 MIT 许可证下开源，因此可以自由使用、自托管和修改。</p>
      <p><strong>Marp、reveal.js 与 Slidev——我该用哪个？</strong>要最简单的纯 Markdown 幻灯片以及便捷的 PDF/pptx 导出，选 Marp；要一个成熟、以 HTML 为先、可深度定制的网页幻灯片框架，选 reveal.js；要 Markdown 加上交互式 Vue 组件，选 Slidev。这三者都采用 MIT 许可证，都无法直接导出可编辑的 PowerPoint。</p>
      <h2>结语</h2>
      <p>Marp 是把你已有的 Markdown 变成幻灯片最干净利落的方式——一个 <code>.md</code> 文件、CSS 主题，以及一个能导出 HTML、PDF 和 PowerPoint 的 CLI。只要记住那一个诚实的限制：pptx 导出来是图片，所以把它当成交付格式，而不是你会在 PowerPoint 里继续编辑的东西。如果你的需求是"我有 Markdown，给我一份幻灯片"，Marp 很难被超越。而当幻灯片需要被生成、保持品牌调性，并在一套更大的设计工作流里保持可编辑时，那就是 <a href="/">Agent-Native Design Workspace</a> 接手的地方。</p>
  ja:
    title: 'Marp：Markdown プレゼンテーションツール（そして PowerPoint への書き出し方法）'
    summary: 'Marp はオープンソース（MIT）の Markdown プレゼンテーションエコシステムです。Markdown でスライドを書き、Marp CLI で HTML、PDF、PowerPoint に書き出せます。本ガイドでは、Marp のテーマ、Markdown での執筆、marp-cli による pptx（画像として）への書き出し、reveal.js や Slidev との比較、そして Agent-Native Design Workspace が活きる場面を解説します。'
    category: 'ツールとスキル'
    bodyHtml: |
      <p>「marp」を歯科や矯正の用語だと思って検索したなら、これはそれとは別物です。<strong>Marp は Markdown プレゼンテーションエコシステム（Markdown Presentation Ecosystem）</strong>——GitHub の <code>marp-team</code> による、プレーンな Markdown を書いてスライドデッキを作るためのオープンソース（MIT）プロジェクトです。キャンバス上でボックスをドラッグすることも、独自のファイル形式もありません。<code># Slide title</code> といくつかの箇条書きを書き、ディレクティブを一つ二つ加えれば、Marp がそれをプレゼンテーションに変えてくれます。（見かけたかもしれない無関係な「MARPE」は矯正歯科の上顎拡大術のことで、スライドとは何の関係もありません。本記事はプレゼンテーションツールの Marp だけを扱います。）</p>
      <p>オープンソースのプレゼンテーションツールの中で、Marp は最もシンプルで最も Markdown ネイティブな選択肢であり、まさにそれが開発者に選ばれる理由です。本記事は、Marp とは何か、執筆とテーマの仕組み、Marp CLI がどう PowerPoint と PDF に書き出すのか（そして人がつまずく一つの注意点）、reveal.js や Slidev との比較、そして Markdown が行き詰まったときにデザインワークスペースがどこで引き継ぐのかを、正直に解説するガイドです。</p>
      <h2>Marp とは</h2>
      <p>Marp は一つの発想を中心に構築された小さなツール群です：<strong>スライドは Markdown</strong>。三つの構成要素があります。<strong>Marpit</strong> は、Markdown とテーマをスライドの HTML と CSS に変換する低レベルのフレームワークです。<strong>Marp Core</strong> は Marpit の上に載る、デフォルトテーマ・画像処理・数式サポートを備えた「電池同梱」のエンジンです。<strong>Marp CLI</strong>（<code>marp-team/marp-cli</code>）は、あなたの Markdown ファイルを HTML、PDF、PowerPoint にレンダリングするコマンドラインツールです。さらに、入力しながらリアルタイムで横並びプレビューを見せてくれる公式の <strong>VS Code 拡張</strong>もあります。</p>
      <p>執筆は一つの <code>.md</code> ファイルで行います。スライドは <code>---</code> で区切り、スライドごとおよびグローバルの設定は Marp のディレクティブ（<code>theme:</code>、<code>paginate:</code>、<code>class:</code> など）で指定し、すべてがバージョン管理可能なプレーンテキストのままです。それこそが最大の魅力です。あなたのデッキは Git できれいに差分が取れ、コードの隣に置け、バイナリ形式に縛られることもありません。</p>
      <h2>Marp のテーマと Markdown での執筆</h2>
      <p>Marp デッキの執筆は意図的に最小限です。ファイル冒頭の front-matter ブロックがグローバルディレクティブを設定し、<code>---</code> がスライドを区切り、標準の Markdown——見出し、リスト、コードフェンス、画像、表——がそのままコンテンツになります。Marp は内部で HTML と CSS にレンダリングするため、Markdown で表現できるものは追加の構文を覚えることなくスライドに対応します。</p>
      <p><strong>Marp のテーマ</strong>はただの CSS です。Marp Core はいくつかの組み込みテーマ（<code>default</code>、<code>gaia</code>、<code>uncover</code>）を同梱しており、たった一つの <code>theme:</code> ディレクティブで切り替えられます。組み込みでは足りないときは、自分のテーマを CSS ファイルとして書きます——Marpit が公開するクラスを対象にし、<code>--theme</code> または <code>--theme-set</code> で CLI に登録すれば、すべてのスライドがそれを取り込みます。インラインの微調整には、一枚のスライドにスコープ付きの <code>&lt;style&gt;</code> ブロックを差し込めます。この CSS ネイティブなモデルこそが Marp の天井であり床でもあります。CSS に慣れていれば完全に制御でき、そうでなくてもデフォルトのままきれいに見えます。</p>
      <h2>Marp を PowerPoint と PDF に書き出す</h2>
      <p>書き出しこそ Marp CLI が真価を発揮する場面です。Markdown を指定し、ターゲットを選びます：</p>
      <ul><li><strong>HTML</strong>——<code>marp deck.md -o deck.html</code> は、ブラウザで発表できる自己完結型の Web デッキを生成します。</li><li><strong>PDF</strong>——<code>marp deck.md --pdf</code> は印刷対応の PDF をレンダリングし、配布資料や共有に最適です。</li><li><strong>PowerPoint</strong>——<code>marp deck.md --pptx</code> は、PowerPoint や Keynote で開ける <code>.pptx</code> ファイルを書き出します。</li></ul>
      <p>はっきり述べておくべき一つの注意点があります：<strong>Marp の PPTX 書き出しは各スライドを画像としてレンダリングします</strong>。ネイティブで編集可能な PowerPoint の図形ではありません。すべてのスライドは <code>.pptx</code> ラッパーの中で全面の画像になります。PowerPoint から離れられない、発表するだけの相手にファイルを渡すときには完璧です——しかし書き出した後は、テキストボックスに入って編集したり、図形の色を変えたり、箇条書きを動かしたりはできません。あとで PowerPoint で編集し続ける必要があるワークフローなら、Marp の pptx はソース形式ではなく配布形式として扱いましょう。（実験的な「編集可能」書き出しモードは現れては消えてきました。信頼でき、文書化された挙動は一スライド一画像です。）</p>
      <h2>Marp と Slidev、reveal.js の比較</h2>
      <p>Marp は Markdown ファーストのプレゼンテーションツールの唯一の選択肢ではありません。選択を左右する観点で、正直に比較してみましょう：</p>
      <table><thead><tr><th>ツール</th><th>執筆方法</th><th>編集可能な PPTX 書き出し</th><th>テンプレート化</th><th>ライセンス</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>純粋な Markdown + ディレクティブ</td><td>pptx は画像（編集可能な図形ではない）</td><td>CSS テーマ</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML（Markdown プラグインは任意）</td><td>ネイティブ pptx なし</td><td>HTML/CSS テーマ</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + Vue コンポーネント</td><td>ネイティブ pptx なし（PDF/PNG/SPA）</td><td>Vue + Windi/UnoCSS テーマ</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>エージェントに説明する</td><td>デザインシステムに沿って編集可能なデッキ</td><td>デザインシステム + テンプレートライブラリ</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> は、最も手間をかけたくないとき——一つの Markdown ファイルと CLI だけ——に選ぶツールです。<strong><a href="/blog/reveal-js/">reveal.js</a></strong> は成熟した Web スライドフレームワークです。HTML ファーストで、際限なくカスタマイズでき、ブラウザ内で最も豊かな体験を提供しますが、書くのは HTML で、ネイティブの PowerPoint 経路はありません。<strong><a href="/blog/slidev/">Slidev</a></strong> はその中間で、Markdown に加えて本物の Vue コンポーネント、コードハイライト、インタラクティブな埋め込みを求める開発者向けです——Marp より強力ですが、セットアップも多くなります。三つとも MIT ライセンスで、いずれも PowerPoint で直接編集できるものは書き出しません。</p>
      <h2>Open Design が活きる場面</h2>
      <p>Marp はまさに一つのことに秀でています：あなたがすでに書いた Markdown をスライドに変えることです。しかしそれは<em>あなた</em>にデッキを書くこと——構成、言い回し、テーマ CSS——を求めます。コンテンツを生成したり、プロジェクトをまたいでブランドに沿って保ったり、書き出した後に編集可能な成果物を渡したりはしません。</p>
      <p><a href="/">Open Design</a> はその一段上のレイヤーです。すでに使っているコーディングエージェントの外側に位置する、オープンソース（Apache-2.0）、ローカルファースト、キー持ち込み方式の <strong>Agent-Native Design Workspace</strong> です。Markdown を手書きする代わりに、欲しいデッキを説明すると、エージェントがデザインシステムに沿って編集可能なものを生成します——出発点となるファーストパーティの HTML デッキテンプレートを<a href="/plugins/templates/">プラグインライブラリ</a>に備えています。これは Markdown で書く Marp の置き換えではありません。すでに Markdown にコンテンツがあり、コマンドラインから手早くデッキがほしいなら、Marp が適切なツールです。デッキを生成させ、ブランドに沿わせ、より大きなデザインワークフローの一部として編集可能なままにしたいなら、それはワークスペースの仕事です。すでにコーディングエージェントでスライドを作っているなら、<a href="/blog/claude-ppt-skills/">コーディングエージェントでのスライド作成方法</a>をご覧ください。</p>
      <h2>FAQ</h2>
      <p><strong>Marp とは？</strong> Marp は GitHub の <code>marp-team</code> によるオープンソースの Markdown プレゼンテーションエコシステムです。Markdown でスライドを書くと、Marp CLI がそれを HTML、PDF、PowerPoint に書き出します。MIT ライセンスで、Marp Core、Marpit フレームワーク、VS Code 拡張を含みます。矯正歯科の「MARPE」という用語とは関係ありません。</p>
      <p><strong>Marp デッキを PowerPoint に書き出すには？</strong> Marp CLI（<code>marp-cli</code>）をインストールし、<code>marp deck.md --pptx</code> を実行します。書き出された <code>.pptx</code> は各スライドを画像としてレンダリングし、ネイティブで編集可能な PowerPoint の図形ではない点に注意してください——受け渡しには最適ですが、その後 PowerPoint 内でテキストや図形を編集することはできません。</p>
      <p><strong>Marp は編集可能な PowerPoint ファイルを書き出せますか？</strong> 確実にはできません。Marp の文書化された PPTX の挙動は一スライド一画像です。本物のテキストボックスや図形を持つネイティブに編集可能な <code>.pptx</code> が必要なら、その工程には Marp は不向きです。</p>
      <p><strong>Marp は無料ですか？</strong> はい。Marp は MIT ライセンスのオープンソースなので、無料で使え、セルフホストでき、改変もできます。</p>
      <p><strong>Marp、reveal.js、Slidev——どれを使うべき？</strong> 最もシンプルな純 Markdown デッキと手軽な PDF/pptx 書き出しなら Marp、深いカスタマイズができる成熟した HTML ファーストの Web スライドフレームワークなら reveal.js、Markdown に加えてインタラクティブな Vue コンポーネントなら Slidev を選びましょう。三つとも MIT ライセンスで、いずれも編集可能な PowerPoint を直接は書き出しません。</p>
      <h2>まとめ</h2>
      <p>Marp は、すでに手元にある Markdown をスライドに変える最もすっきりした方法です——一つの <code>.md</code> ファイル、CSS テーマ、そして HTML、PDF、PowerPoint を書き出す CLI。ただ一つの正直な制約だけは忘れないでください：pptx は画像として出てくるので、PowerPoint で編集し続けるものではなく配布形式として扱いましょう。「Markdown はある、デッキがほしい」というニーズなら、Marp を超えるものはなかなかありません。デッキを生成させ、ブランドに沿わせ、より大きなデザインワークフローの中で編集可能なままにする必要があるとき——そこで <a href="/">Agent-Native Design Workspace</a> が引き継ぎます。</p>
  ko:
    title: 'Marp: 마크다운 프레젠테이션 도구(그리고 PowerPoint로 내보내는 방법)'
    summary: 'Marp는 오픈소스 마크다운 프레젠테이션 생태계입니다. 마크다운으로 슬라이드를 작성한 뒤 Marp CLI로 HTML, PDF, PowerPoint로 내보낼 수 있습니다. 이 가이드에서는 Marp 테마, 마크다운 작성, marp-cli로 pptx(이미지 형태) 내보내기, reveal.js·Slidev와의 비교, 그리고 Agent-Native Design Workspace가 어울리는 지점을 다룹니다.'
    category: '도구와 스킬'
    bodyHtml: |
      <p>"marp"를 치과나 교정 용어로 생각하고 검색했다면, 이건 그것과는 다른 쪽입니다. <strong>Marp는 Markdown 프레젠테이션 생태계(Markdown Presentation Ecosystem)</strong>로, GitHub의 <code>marp-team</code>이 만든, 순수 Markdown을 작성해 슬라이드 덱을 만드는 오픈소스(MIT) 프로젝트입니다. 캔버스에서 상자를 끌어다 놓을 필요도, 독점 파일 형식도 없습니다. <code># Slide title</code>과 몇 개의 불릿을 쓰고 디렉티브 한두 개를 더하면, Marp가 그것을 프레젠테이션으로 바꿔 줍니다. (봤을지도 모를, 관련 없는 "MARPE"는 교정치과의 상악 확장술로 슬라이드와는 아무 상관이 없습니다. 이 글은 오직 프레젠테이션 도구 Marp만 다룹니다.)</p>
      <p>오픈소스 프레젠테이션 도구 중에서 Marp는 가장 단순하고 가장 Markdown에 가까운 선택지이며, 바로 그 점 때문에 개발자들이 손을 뻗습니다. 이 글은 Marp가 무엇인지, 작성과 테마가 어떻게 작동하는지, Marp CLI가 PowerPoint와 PDF로 어떻게 내보내는지(그리고 사람들이 걸려 넘어지는 한 가지 주의점), reveal.js·Slidev와 어떻게 비교되는지, 그리고 Markdown이 한계에 부딪힐 때 디자인 워크스페이스가 어디서 이어받는지를 솔직하게 정리한 가이드입니다.</p>
      <h2>Marp란</h2>
      <p>Marp는 하나의 발상을 중심으로 만들어진 작은 도구 모음입니다: <strong>슬라이드는 Markdown</strong>. 세 가지 구성 요소가 있습니다. <strong>Marpit</strong>은 Markdown과 테마를 슬라이드 HTML과 CSS로 바꾸는 저수준 프레임워크입니다. <strong>Marp Core</strong>는 Marpit 위에 얹힌, 기본 테마·이미지 처리·수식 지원을 갖춘 올인원 엔진입니다. <strong>Marp CLI</strong>(<code>marp-team/marp-cli</code>)는 당신의 Markdown 파일을 HTML, PDF, PowerPoint로 렌더링하는 명령줄 도구입니다. 입력하는 대로 실시간 나란히 미리보기를 보여 주는 공식 <strong>VS Code 확장</strong>도 있습니다.</p>
      <p>작성은 하나의 <code>.md</code> 파일에서 합니다. 슬라이드는 <code>---</code>로 구분하고, 슬라이드별·전역 설정은 Marp 디렉티브(<code>theme:</code>, <code>paginate:</code>, <code>class:</code> 등)로 지정하며, 모든 것이 버전 관리가 가능한 순수 텍스트로 남습니다. 바로 그것이 전부의 매력입니다. 당신의 덱은 Git에서 깔끔하게 diff되고, 코드 옆에 놓이며, 결코 바이너리 형식에 갇히지 않습니다.</p>
      <h2>Marp 테마와 Markdown 작성</h2>
      <p>Marp 덱 작성은 의도적으로 최소한입니다. 파일 맨 위의 front-matter 블록이 전역 디렉티브를 설정하고, <code>---</code>가 슬라이드를 나누며, 표준 Markdown——제목, 목록, 코드 펜스, 이미지, 표——이 그대로 콘텐츠가 됩니다. Marp는 내부적으로 HTML과 CSS로 렌더링하므로, Markdown이 표현할 수 있는 것은 추가 문법을 배울 필요 없이 슬라이드로 매핑됩니다.</p>
      <p><strong>Marp 테마</strong>는 그저 CSS입니다. Marp Core는 몇 가지 내장 테마(<code>default</code>, <code>gaia</code>, <code>uncover</code>)를 제공하며, <code>theme:</code> 디렉티브 하나로 그 사이를 전환합니다. 내장 테마가 부족할 때는 자신의 테마를 CSS 파일로 작성합니다——Marpit이 노출하는 클래스를 대상으로 하고, <code>--theme</code>나 <code>--theme-set</code>으로 CLI에 등록하면 모든 슬라이드가 그것을 적용합니다. 인라인 조정을 위해서는 한 슬라이드에 스코프가 지정된 <code>&lt;style&gt;</code> 블록을 넣을 수 있습니다. 이 CSS 네이티브 모델이 Marp의 천장이자 바닥입니다. CSS에 익숙하면 완전한 제어권을 갖고, 그렇지 않아도 기본값은 여전히 깔끔해 보입니다.</p>
      <h2>Marp를 PowerPoint와 PDF로 내보내기</h2>
      <p>내보내기야말로 Marp CLI가 제 몫을 하는 지점입니다. Markdown을 가리키고 대상 형식을 고르세요:</p>
      <ul><li><strong>HTML</strong>——<code>marp deck.md -o deck.html</code>은 브라우저에서 발표하는 자체 완결형 웹 덱을 생성합니다.</li><li><strong>PDF</strong>——<code>marp deck.md --pdf</code>는 인쇄에 적합한 PDF를 렌더링하며, 유인물과 공유에 이상적입니다.</li><li><strong>PowerPoint</strong>——<code>marp deck.md --pptx</code>는 PowerPoint나 Keynote에서 열 수 있는 <code>.pptx</code> 파일을 내보냅니다.</li></ul>
      <p>분명히 말해 둘 한 가지 주의점: <strong>Marp의 PPTX 내보내기는 각 슬라이드를 이미지로 렌더링합니다</strong>. 네이티브하고 편집 가능한 PowerPoint 도형이 아닙니다. 모든 슬라이드는 <code>.pptx</code> 래퍼 안에서 전면 이미지가 됩니다. PowerPoint에서 사는, 발표만 하면 되는 사람에게 파일을 넘겨야 할 때는 완벽합니다——하지만 내보낸 뒤에는 텍스트 상자를 클릭해 편집하거나, 도형 색을 바꾸거나, 불릿을 옮길 수 없습니다. 워크플로가 이후에도 PowerPoint에서 계속 편집해야 한다면, Marp의 pptx를 소스 형식이 아니라 전달 형식으로 다루세요. (실험적인 "편집 가능" 내보내기 모드는 왔다가 사라졌습니다. 신뢰할 수 있고 문서화된 동작은 슬라이드당 이미지 하나입니다.)</p>
      <h2>Marp vs Slidev vs reveal.js</h2>
      <p>Marp가 Markdown 우선 프레젠테이션 도구의 유일한 선택지는 아닙니다. 당신의 선택을 좌우하는 축들을 기준으로 한 솔직한 비교입니다:</p>
      <table><thead><tr><th>도구</th><th>작성 방식</th><th>편집 가능한 PPTX 내보내기</th><th>템플릿화</th><th>라이선스</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>순수 Markdown + 디렉티브</td><td>pptx는 이미지(편집 가능한 도형 아님)</td><td>CSS 테마</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML(Markdown 플러그인 선택)</td><td>네이티브 pptx 없음</td><td>HTML/CSS 테마</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + Vue 컴포넌트</td><td>네이티브 pptx 없음(PDF/PNG/SPA)</td><td>Vue + Windi/UnoCSS 테마</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>에이전트에게 설명</td><td>디자인 시스템에 맞춘 편집 가능한 덱</td><td>디자인 시스템 + 템플릿 라이브러리</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong>는 가장 격식을 덜고 싶을 때——Markdown 파일 하나와 CLI——의 선택입니다. <strong><a href="/blog/reveal-js/">reveal.js</a></strong>는 성숙한 웹 슬라이드 프레임워크입니다. HTML 우선, 끝없이 커스터마이즈 가능하며 브라우저 내에서 가장 풍부한 경험을 주지만, 당신이 쓰는 것은 HTML이고 네이티브 PowerPoint 경로는 없습니다. <strong><a href="/blog/slidev/">Slidev</a></strong>는 그 중간으로, Markdown에 더해 진짜 Vue 컴포넌트, 코드 하이라이트, 인터랙티브 임베드를 원하는 개발자를 위한 것입니다——Marp보다 강력하지만 설정도 더 많습니다. 셋 다 MIT 라이선스이며, 어느 것도 PowerPoint에서 바로 편집 가능한 것을 내보내지 않습니다.</p>
      <h2>Open Design이 어울리는 지점</h2>
      <p>Marp는 딱 한 가지에 탁월합니다: 당신이 이미 작성한 Markdown을 슬라이드로 바꾸는 것. 하지만 그것은 <em>당신</em>에게 덱을 쓰라고 요구합니다——구조, 문구, 테마 CSS. 콘텐츠를 생성하거나, 프로젝트 전반에 걸쳐 브랜드에 맞게 유지하거나, 내보낸 뒤 편집 가능한 산출물을 주지는 않습니다.</p>
      <p><a href="/">Open Design</a>은 그 위의 레이어입니다. 이미 사용 중인 코딩 에이전트 바깥에 자리한, 오픈소스(Apache-2.0)이고 로컬 우선이며 키 지참 방식의 <strong>Agent-Native Design Workspace</strong>입니다. Markdown을 손으로 쓰는 대신 원하는 덱을 설명하면, 에이전트가 디자인 시스템에 맞춰 편집 가능한 덱을 생성합니다——출발점이 되는 퍼스트파티 HTML 덱 템플릿을 <a href="/plugins/templates/">플러그인 라이브러리</a>에 갖추고 있습니다. 이는 Markdown으로 작성하는 Marp의 대체품이 아닙니다. 이미 Markdown에 콘텐츠가 있고 명령줄에서 빠른 덱을 원한다면 Marp가 알맞은 도구입니다. 덱이 생성되고, 브랜드에 맞고, 더 큰 디자인 워크플로의 일부로 여전히 편집 가능하길 원한다면, 그건 워크스페이스의 몫입니다. 이미 코딩 에이전트로 슬라이드를 만든다면 <a href="/blog/claude-ppt-skills/">코딩 에이전트로 슬라이드 덱 만드는 법</a>을 참고하세요.</p>
      <h2>자주 묻는 질문</h2>
      <p><strong>Marp란 무엇인가요?</strong> Marp는 GitHub의 <code>marp-team</code>이 만든 오픈소스 Markdown 프레젠테이션 생태계입니다. Markdown으로 슬라이드를 작성하면 Marp CLI가 그것을 HTML, PDF, PowerPoint로 내보냅니다. MIT 라이선스이며 Marp Core, Marpit 프레임워크, VS Code 확장을 포함합니다. 교정치과의 "MARPE" 용어와는 무관합니다.</p>
      <p><strong>Marp 덱을 PowerPoint로 어떻게 내보내나요?</strong> Marp CLI(<code>marp-cli</code>)를 설치하고 <code>marp deck.md --pptx</code>를 실행하세요. 내보낸 <code>.pptx</code>는 각 슬라이드를 이미지로 렌더링하며, 네이티브하고 편집 가능한 PowerPoint 도형이 아니라는 점에 유의하세요——전달에는 훌륭하지만 이후 PowerPoint 안에서 텍스트나 도형을 편집할 수는 없습니다.</p>
      <p><strong>Marp는 편집 가능한 PowerPoint 파일을 내보낼 수 있나요?</strong> 확실하게는 아닙니다. Marp의 문서화된 PPTX 동작은 슬라이드당 이미지 하나입니다. 진짜 텍스트 상자와 도형을 갖춘 네이티브 편집 가능한 <code>.pptx</code>가 필요하다면, 그 단계에는 Marp가 맞지 않는 도구입니다.</p>
      <p><strong>Marp는 무료인가요?</strong> 네. Marp는 MIT 라이선스 오픈소스이므로 자유롭게 사용하고, 셀프호스팅하고, 수정할 수 있습니다.</p>
      <p><strong>Marp vs reveal.js vs Slidev——무엇을 써야 하나요?</strong> 가장 단순한 순수 Markdown 덱과 손쉬운 PDF/pptx 내보내기를 원하면 Marp, 깊은 커스터마이즈가 가능한 성숙한 HTML 우선 웹 슬라이드 프레임워크를 원하면 reveal.js, Markdown에 더해 인터랙티브 Vue 컴포넌트를 원하면 Slidev를 고르세요. 셋 다 MIT 라이선스이며, 어느 것도 편집 가능한 PowerPoint를 바로 내보내지 않습니다.</p>
      <h2>핵심 요약</h2>
      <p>Marp는 이미 가진 Markdown을 슬라이드로 바꾸는 가장 깔끔한 방법입니다——하나의 <code>.md</code> 파일, CSS 테마, 그리고 HTML, PDF, PowerPoint를 내보내는 CLI. 다만 한 가지 솔직한 한계는 기억하세요: pptx는 이미지로 나오므로, PowerPoint에서 계속 편집할 것이 아니라 전달 형식으로 다루세요. 필요가 "Markdown은 있으니 덱을 달라"라면 Marp를 이기기는 어렵습니다. 덱이 생성되고, 브랜드에 맞고, 더 큰 디자인 워크플로 안에서 편집 가능해야 할 때——그때 <a href="/">Agent-Native Design Workspace</a>가 이어받습니다.</p>
  de:
    title: 'Marp: Das Markdown-Präsentationstool (und wie man nach PowerPoint exportiert)'
    summary: 'Marp ist das quelloffene Markdown-Präsentationsökosystem – schreibe Folien in Markdown und exportiere sie dann mit der Marp CLI nach HTML, PDF oder PowerPoint. Dieser Leitfaden behandelt Marp-Themes, das Verfassen in Markdown, den Export nach pptx (als Bilder) mit marp-cli, den Vergleich mit reveal.js und Slidev sowie die Frage, wo ein Agent-Native Design Workspace ansetzt.'
    category: 'Tools & Skills'
    bodyHtml: |
      <p>Wenn Sie nach „marp" gesucht haben und einen zahnmedizinischen oder kieferorthopädischen Begriff erwarteten, ist dies das andere: <strong>Marp ist das Markdown Presentation Ecosystem</strong> – ein quelloffenes (MIT) Projekt des <code>marp-team</code> auf GitHub, mit dem man Foliensätze durch das Schreiben von reinem Markdown erstellt. Kein Ziehen von Kästchen auf einer Leinwand, kein proprietäres Dateiformat. Sie schreiben <code># Slide title</code> und ein paar Aufzählungspunkte, fügen ein oder zwei Direktiven hinzu, und Marp macht daraus eine Präsentation. (Das unverwandte „MARPE", das Ihnen begegnet sein könnte, ist ein Verfahren zur Oberkiefererweiterung in der Kieferorthopädie – es hat nichts mit Folien zu tun. Dieser Artikel handelt ausschließlich von Marp, dem Präsentationswerkzeug.)</p>
      <p>Marp ist unter den quelloffenen Präsentationswerkzeugen die einfachste, am stärksten Markdown-native Option, und genau deshalb greifen Entwickler dazu. Dies ist ein ehrlicher Leitfaden dazu, was Marp ist, wie das Verfassen und Themes funktionieren, wie die Marp CLI nach PowerPoint und PDF exportiert (und der eine Vorbehalt, der viele überrascht), wie es sich gegen reveal.js und Slidev schlägt, und wo ein Design-Workspace übernimmt, wenn Markdown an seine Grenzen stößt.</p>
      <h2>Was Marp ist</h2>
      <p>Marp ist ein kleines Ökosystem von Werkzeugen rund um eine Idee: <strong>Folien als Markdown</strong>. Es hat drei bewegliche Teile. <strong>Marpit</strong> ist das Low-Level-Framework, das Markdown plus ein Theme in Folien-HTML und -CSS verwandelt. <strong>Marp Core</strong> ist die sofort einsatzbereite Engine auf Marpit, mit einem Standard-Theme, Bildverarbeitung und Formelunterstützung. <strong>Marp CLI</strong> (<code>marp-team/marp-cli</code>) ist das Kommandozeilenwerkzeug, das Ihre Markdown-Datei in HTML, PDF oder PowerPoint rendert. Es gibt außerdem eine offizielle <strong>VS-Code-Erweiterung</strong>, die Ihnen beim Tippen eine Live-Vorschau nebeneinander zeigt.</p>
      <p>Sie verfassen alles in einer <code>.md</code>-Datei. Folien werden durch <code>---</code> getrennt, folien- und globale Einstellungen werden über Marp-Direktiven (wie <code>theme:</code>, <code>paginate:</code> oder <code>class:</code>) gesetzt, und alles bleibt versionierbarer Klartext. Das ist der ganze Reiz: Ihr Foliensatz lässt sich in Git sauber diffen, liegt neben Ihrem Code und sperrt Sie nie in ein Binärformat ein.</p>
      <h2>Marp-Themes und das Verfassen in Markdown</h2>
      <p>Das Verfassen eines Marp-Foliensatzes ist bewusst minimal. Ein Front-Matter-Block am Anfang der Datei setzt globale Direktiven, <code>---</code> trennt Folien, und standardmäßiges Markdown – Überschriften, Listen, Code-Fences, Bilder, Tabellen – wird zu Ihrem Inhalt. Da Marp unter der Haube nach HTML und CSS rendert, lässt sich alles, was Markdown ausdrückt, ohne zusätzliche Syntax auf eine Folie abbilden.</p>
      <p><strong>Marp-Themes</strong> sind einfach CSS. Marp Core bringt eine Handvoll integrierter Themes mit (<code>default</code>, <code>gaia</code>, <code>uncover</code>), und Sie wechseln zwischen ihnen mit einer einzigen <code>theme:</code>-Direktive. Wenn die integrierten nicht ausreichen, schreiben Sie Ihr eigenes Theme als CSS-Datei – zielen Sie auf die von Marpit bereitgestellten Klassen, registrieren Sie es über die CLI mit <code>--theme</code> oder <code>--theme-set</code>, und jede Folie übernimmt es. Für Inline-Feinabstimmungen können Sie einen gescopten <code>&lt;style&gt;</code>-Block in eine einzelne Folie einfügen. Dieses CSS-native Modell ist Marps eigentliche Decke und zugleich Boden: Wenn Sie mit CSS vertraut sind, haben Sie volle Kontrolle, und wenn nicht, sehen die Standards trotzdem sauber aus.</p>
      <h2>Marp nach PowerPoint und PDF exportieren</h2>
      <p>Beim Export verdient sich die Marp CLI ihren Platz. Richten Sie sie auf Ihr Markdown und wählen Sie ein Ziel:</p>
      <ul><li><strong>HTML</strong> – <code>marp deck.md -o deck.html</code> erzeugt einen eigenständigen Web-Foliensatz, den Sie im Browser präsentieren.</li><li><strong>PDF</strong> – <code>marp deck.md --pdf</code> rendert ein druckfertiges PDF, ideal für Handouts und zum Teilen.</li><li><strong>PowerPoint</strong> – <code>marp deck.md --pptx</code> exportiert eine <code>.pptx</code>-Datei, die Sie in PowerPoint oder Keynote öffnen können.</li></ul>
      <p>Der eine Vorbehalt, den man klar aussprechen sollte: <strong>Marps PPTX-Export rendert jede Folie als Bild</strong>, nicht als native, bearbeitbare PowerPoint-Formen. Jede Folie wird zu einem randlosen Bild innerhalb der <code>.pptx</code>-Hülle. Das ist perfekt, wenn Sie eine Datei an jemanden übergeben müssen, der in PowerPoint lebt und sie nur präsentieren muss – aber nach dem Export können Sie kein Textfeld anklicken und bearbeiten, keine Form umfärben und keinen Aufzählungspunkt verschieben. Wenn Ihr Workflow danach in PowerPoint weiterbearbeiten muss, behandeln Sie Marps pptx als Auslieferungsformat, nicht als Quellformat. (Experimentelle „bearbeitbare" Exportmodi sind gekommen und wieder gegangen; das zuverlässige, dokumentierte Verhalten ist ein Bild pro Folie.)</p>
      <h2>Marp vs. Slidev vs. reveal.js</h2>
      <p>Marp ist nicht das einzige Markdown-first-Präsentationswerkzeug. Hier der ehrliche Vergleich entlang der Achsen, die Ihre Wahl entscheiden:</p>
      <table><thead><tr><th>Werkzeug</th><th>Verfassen</th><th>Bearbeitbarer PPTX-Export</th><th>Templating</th><th>Lizenz</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>Reines Markdown + Direktiven</td><td>pptx als Bilder (keine bearbeitbaren Formen)</td><td>CSS-Themes</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML (Markdown-Plugin optional)</td><td>Kein natives pptx</td><td>HTML/CSS-Themes</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + Vue-Komponenten</td><td>Kein natives pptx (PDF/PNG/SPA)</td><td>Vue + Windi/UnoCSS-Themes</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>Beschreiben Sie es Ihrem Agenten</td><td>Bearbeitbarer Foliensatz auf Basis eines Designsystems</td><td>Designsystem + Template-Bibliothek</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> ist die Wahl, wenn Sie den geringsten Aufwand wollen – eine einzige Markdown-Datei und eine CLI. <strong><a href="/blog/reveal-js/">reveal.js</a></strong> ist das ausgereifte Web-Folien-Framework: HTML-first, endlos anpassbar und mit dem reichsten Erlebnis im Browser, aber Sie schreiben HTML, und es gibt keinen nativen PowerPoint-Weg. <strong><a href="/blog/slidev/">Slidev</a></strong> liegt dazwischen, für Entwickler, die Markdown plus echte Vue-Komponenten, Code-Hervorhebung und interaktive Einbettungen wollen – mehr Leistung als Marp, aber auch mehr Einrichtung. Alle drei stehen unter MIT-Lizenz und exportieren nichts, was in PowerPoint direkt bearbeitbar wäre.</p>
      <h2>Wo Open Design ansetzt</h2>
      <p>Marp ist genau in einer Sache hervorragend: bereits geschriebenes Markdown zu nehmen und in Folien zu verwandeln. Aber es verlangt von <em>Ihnen</em>, den Foliensatz zu schreiben – die Struktur, die Formulierung, das Theme-CSS. Es generiert nicht den Inhalt, hält ihn nicht projektübergreifend markenkonform und gibt Ihnen nach dem Export kein bearbeitbares Artefakt.</p>
      <p><a href="/">Open Design</a> ist die Schicht darüber. Es ist ein quelloffener (Apache-2.0), lokal-first, Bring-your-own-Key-<strong>Agent-Native Design Workspace</strong>, der außerhalb des Coding-Agenten sitzt, den Sie bereits nutzen. Statt Markdown von Hand zu schreiben, beschreiben Sie den gewünschten Foliensatz, und der Agent generiert einen bearbeitbaren auf Basis eines Designsystems – mit erstanbieterlichen HTML-Foliensatz-Templates in seiner <a href="/plugins/templates/">Plugin-Bibliothek</a> als Ausgangspunkt. Es ist kein Ersatz für in Markdown verfasstes Marp: Wenn Sie den Inhalt bereits in Markdown haben und schnell einen Foliensatz von der Kommandozeile wollen, ist Marp das richtige Werkzeug. Wenn Sie den Foliensatz generiert, markenkonform und weiterhin bearbeitbar als Teil eines größeren Design-Workflows haben wollen, ist das die Aufgabe des Workspace. Wenn Sie bereits Folien mit einem Coding-Agenten erstellen, sehen Sie sich an, <a href="/blog/claude-ppt-skills/">wie ein Coding-Agent Foliensätze erstellt</a>.</p>
      <h2>FAQ</h2>
      <p><strong>Was ist Marp?</strong> Marp ist das quelloffene Markdown Presentation Ecosystem des <code>marp-team</code> auf GitHub. Sie schreiben Folien in Markdown, und die Marp CLI exportiert sie nach HTML, PDF oder PowerPoint. Es steht unter MIT-Lizenz und umfasst Marp Core, das Marpit-Framework und eine VS-Code-Erweiterung. Es hat nichts mit dem kieferorthopädischen Begriff „MARPE" zu tun.</p>
      <p><strong>Wie exportiere ich einen Marp-Foliensatz nach PowerPoint?</strong> Installieren Sie die Marp CLI (<code>marp-cli</code>) und führen Sie <code>marp deck.md --pptx</code> aus. Beachten Sie, dass die exportierte <code>.pptx</code> jede Folie als Bild rendert, nicht als native bearbeitbare PowerPoint-Formen – ideal für die Übergabe, aber Sie können danach Text oder Formen in PowerPoint nicht bearbeiten.</p>
      <p><strong>Kann Marp bearbeitbare PowerPoint-Dateien exportieren?</strong> Nicht zuverlässig. Marps dokumentiertes PPTX-Verhalten ist ein Bild pro Folie. Wenn Sie nativ bearbeitbares <code>.pptx</code> mit echten Textfeldern und Formen brauchen, ist Marp für diesen Schritt das falsche Werkzeug.</p>
      <p><strong>Ist Marp kostenlos?</strong> Ja. Marp ist quelloffen unter der MIT-Lizenz, also frei nutzbar, selbst hostbar und veränderbar.</p>
      <p><strong>Marp vs. reveal.js vs. Slidev – was sollte ich verwenden?</strong> Wählen Sie Marp für die einfachsten reinen Markdown-Foliensätze und einen leichten PDF-/pptx-Export, reveal.js für ein ausgereiftes HTML-first-Web-Folien-Framework mit tiefer Anpassbarkeit und Slidev für Markdown plus interaktive Vue-Komponenten. Alle drei stehen unter MIT-Lizenz; keines exportiert direkt bearbeitbares PowerPoint.</p>
      <h2>Das Fazit</h2>
      <p>Marp ist der sauberste Weg, bereits vorhandenes Markdown in Folien zu verwandeln – eine einzige <code>.md</code>-Datei, CSS-Themes und eine CLI, die nach HTML, PDF und PowerPoint exportiert. Denken Sie nur an die eine ehrliche Einschränkung: Das pptx kommt als Bilder heraus, behandeln Sie es also als Auslieferungsformat und nicht als etwas, das Sie in PowerPoint weiterbearbeiten. Wenn Ihr Bedarf lautet „Ich habe das Markdown, gib mir einen Foliensatz", ist Marp kaum zu schlagen. Wenn der Foliensatz generiert werden, markenkonform bleiben und innerhalb eines größeren Design-Workflows bearbeitbar bleiben soll, dann setzt ein <a href="/">Agent-Native Design Workspace</a> an.</p>
  fr:
    title: 'Marp : l''outil de présentation en Markdown (et comment exporter vers PowerPoint)'
    summary: 'Marp est l''écosystème open source de présentation en Markdown : rédigez vos diapositives en Markdown, puis exportez-les en HTML, PDF ou PowerPoint avec la Marp CLI. Ce guide couvre les thèmes Marp, la rédaction en Markdown, l''export vers pptx (sous forme d''images) avec marp-cli, la comparaison avec reveal.js et Slidev, et la place d''un Agent-Native Design Workspace.'
    category: 'Outils et skills'
    bodyHtml: |
      <p>Si vous avez cherché « marp » en vous attendant à un terme dentaire ou orthodontique, c'est l'autre : <strong>Marp est le Markdown Presentation Ecosystem</strong> – un projet open source (MIT) de l'équipe <code>marp-team</code> sur GitHub, qui permet de construire des diaporamas en écrivant du Markdown pur. Pas de boîtes à faire glisser sur un canevas, pas de format de fichier propriétaire. Vous écrivez <code># Slide title</code> et quelques puces, ajoutez une directive ou deux, et Marp en fait une présentation. (Le « MARPE » sans rapport que vous avez peut-être vu est une procédure d'expansion maxillaire en orthodontie – rien à voir avec les diapositives. Cet article porte strictement sur Marp, l'outil de présentation.)</p>
      <p>Marp est l'option la plus simple et la plus native au Markdown parmi les outils de présentation open source, et c'est précisément pourquoi les développeurs s'y tournent. Voici un guide honnête sur ce qu'est Marp, sur le fonctionnement de la rédaction et des thèmes, sur la façon dont la Marp CLI exporte vers PowerPoint et PDF (et le seul bémol qui prend les gens au dépourvu), sur sa comparaison avec reveal.js et Slidev, et sur l'endroit où un design workspace prend le relais quand le Markdown atteint ses limites.</p>
      <h2>Ce qu'est Marp</h2>
      <p>Marp est un petit écosystème d'outils bâti autour d'une seule idée : <strong>les diapositives en Markdown</strong>. Il comporte trois pièces mobiles. <strong>Marpit</strong> est le framework de bas niveau qui transforme du Markdown accompagné d'un thème en HTML et CSS de diapositives. <strong>Marp Core</strong> est le moteur clé en main posé sur Marpit, avec un thème par défaut, la gestion des images et la prise en charge des formules mathématiques. <strong>Marp CLI</strong> (<code>marp-team/marp-cli</code>) est l'outil en ligne de commande qui rend votre fichier Markdown en HTML, PDF ou PowerPoint. Il existe aussi une <strong>extension VS Code</strong> officielle qui vous offre un aperçu côte à côte en temps réel pendant que vous tapez.</p>
      <p>Vous rédigez dans un seul fichier <code>.md</code>. Les diapositives sont séparées par <code>---</code>, les réglages par diapositive et globaux se définissent via des directives Marp (comme <code>theme:</code>, <code>paginate:</code> ou <code>class:</code>), et tout reste du texte brut versionnable. C'est là tout l'attrait : votre diaporama se diffe proprement dans Git, vit à côté de votre code, et ne vous enferme jamais dans un format binaire.</p>
      <h2>Les thèmes Marp et la rédaction en Markdown</h2>
      <p>Rédiger un diaporama Marp est délibérément minimal. Un bloc de front-matter en haut du fichier définit les directives globales, <code>---</code> sépare les diapositives, et le Markdown standard – titres, listes, blocs de code, images, tableaux – devient votre contenu. Comme Marp effectue en coulisses un rendu en HTML et CSS, tout ce que le Markdown exprime se traduit en une diapositive sans syntaxe supplémentaire à apprendre.</p>
      <p><strong>Les thèmes Marp</strong> ne sont que du CSS. Marp Core livre une poignée de thèmes intégrés (<code>default</code>, <code>gaia</code>, <code>uncover</code>), et vous passez de l'un à l'autre avec une seule directive <code>theme:</code>. Quand les thèmes intégrés ne suffisent plus, vous écrivez le vôtre sous forme de fichier CSS – ciblez les classes que Marpit expose, enregistrez-le auprès de la CLI via <code>--theme</code> ou <code>--theme-set</code>, et chaque diapositive l'adopte. Pour des ajustements en ligne, vous pouvez glisser un bloc <code>&lt;style&gt;</code> à portée limitée dans une seule diapositive. Ce modèle CSS-natif est à la fois le plafond et le plancher de Marp : si vous êtes à l'aise avec le CSS, vous avez un contrôle total, et sinon, les valeurs par défaut restent élégantes.</p>
      <h2>Exporter Marp vers PowerPoint et PDF</h2>
      <p>C'est à l'export que la Marp CLI justifie sa place. Pointez-la vers votre Markdown et choisissez une cible :</p>
      <ul><li><strong>HTML</strong> – <code>marp deck.md -o deck.html</code> produit un diaporama web autonome que vous présentez dans le navigateur.</li><li><strong>PDF</strong> – <code>marp deck.md --pdf</code> rend un PDF prêt à imprimer, idéal pour les documents à distribuer et le partage.</li><li><strong>PowerPoint</strong> – <code>marp deck.md --pptx</code> exporte un fichier <code>.pptx</code> que vous pouvez ouvrir dans PowerPoint ou Keynote.</li></ul>
      <p>Le seul bémol qui mérite d'être dit clairement : <strong>l'export PPTX de Marp rend chaque diapositive sous forme d'image</strong>, et non de formes PowerPoint natives et modifiables. Chaque diapositive devient une image pleine page à l'intérieur de l'enveloppe <code>.pptx</code>. C'est parfait quand vous devez remettre un fichier à quelqu'un qui vit dans PowerPoint et n'a qu'à le présenter – mais une fois exporté, vous ne pouvez pas cliquer pour modifier une zone de texte, recolorer une forme ou déplacer une puce. Si votre flux de travail doit continuer à éditer dans PowerPoint ensuite, traitez le pptx de Marp comme un format de livraison, pas comme un format source. (Des modes d'export « modifiables » expérimentaux sont apparus puis ont disparu ; le comportement fiable et documenté est une image par diapositive.)</p>
      <h2>Marp vs Slidev vs reveal.js</h2>
      <p>Marp n'est pas le seul outil de présentation Markdown-first. Voici la comparaison honnête selon les axes qui décident de votre choix :</p>
      <table><thead><tr><th>Outil</th><th>Rédaction</th><th>Export PPTX modifiable</th><th>Gabarits</th><th>Licence</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>Markdown pur + directives</td><td>pptx en images (pas de formes modifiables)</td><td>Thèmes CSS</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML (plugin Markdown optionnel)</td><td>Pas de pptx natif</td><td>Thèmes HTML/CSS</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + composants Vue</td><td>Pas de pptx natif (PDF/PNG/SPA)</td><td>Thèmes Vue + Windi/UnoCSS</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>Décrivez-le à votre agent</td><td>Diaporama modifiable adossé à un design system</td><td>Design system + bibliothèque de modèles</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> est le choix quand vous voulez le moins de cérémonie – un seul fichier Markdown et une CLI. <strong><a href="/blog/reveal-js/">reveal.js</a></strong> est le framework de diapositives web mature : HTML-first, personnalisable à l'infini et offrant l'expérience la plus riche dans le navigateur, mais vous écrivez du HTML et il n'y a pas de voie PowerPoint native. <strong><a href="/blog/slidev/">Slidev</a></strong> se situe au milieu, pour les développeurs qui veulent du Markdown accompagné de vrais composants Vue, de la coloration syntaxique et des intégrations interactives – plus de puissance que Marp, mais plus de configuration aussi. Les trois sont sous licence MIT et n'exportent rien de directement modifiable dans PowerPoint.</p>
      <h2>La place d'Open Design</h2>
      <p>Marp excelle exactement en une chose : prendre le Markdown que vous avez déjà écrit et le transformer en diapositives. Mais il vous demande, à <em>vous</em>, d'écrire le diaporama – la structure, la formulation, le CSS du thème. Il ne génère pas le contenu, ne le maintient pas cohérent avec votre marque d'un projet à l'autre, et ne vous donne pas d'artéfact modifiable une fois l'export réalisé.</p>
      <p><a href="/">Open Design</a> est la couche au-dessus. C'est un <strong>Agent-Native Design Workspace</strong> open source (Apache-2.0), local-first, avec vos propres clés, qui se situe en dehors de l'agent de code que vous utilisez déjà. Au lieu d'écrire du Markdown à la main, vous décrivez le diaporama voulu, et l'agent en génère un modifiable adossé à un design system – avec des modèles de diaporamas HTML propriétaires dans sa <a href="/plugins/templates/">bibliothèque de plugins</a> comme point de départ. Ce n'est pas un remplacement du Marp rédigé en Markdown : quand vous avez déjà le contenu en Markdown et voulez un diaporama rapide depuis la ligne de commande, Marp est le bon outil. Quand vous voulez que le diaporama soit généré, conforme à la marque et toujours modifiable au sein d'un flux de conception plus large, c'est le rôle du workspace. Si vous créez déjà des diapositives avec un agent de code, voyez <a href="/blog/claude-ppt-skills/">comment un agent de code crée des diaporamas</a>.</p>
      <h2>FAQ</h2>
      <p><strong>Qu'est-ce que Marp ?</strong> Marp est le Markdown Presentation Ecosystem open source de l'équipe <code>marp-team</code> sur GitHub. Vous écrivez des diapositives en Markdown, et la Marp CLI les exporte en HTML, PDF ou PowerPoint. Il est sous licence MIT et inclut Marp Core, le framework Marpit et une extension VS Code. Il n'a aucun rapport avec le terme orthodontique « MARPE ».</p>
      <p><strong>Comment exporter un diaporama Marp vers PowerPoint ?</strong> Installez la Marp CLI (<code>marp-cli</code>) et lancez <code>marp deck.md --pptx</code>. Notez que le <code>.pptx</code> exporté rend chaque diapositive sous forme d'image, et non de formes PowerPoint natives et modifiables – parfait pour la remise, mais vous ne pourrez pas ensuite modifier le texte ou les formes dans PowerPoint.</p>
      <p><strong>Marp peut-il exporter des fichiers PowerPoint modifiables ?</strong> Pas de façon fiable. Le comportement PPTX documenté de Marp est une image par diapositive. Si vous avez besoin d'un <code>.pptx</code> nativement modifiable avec de vraies zones de texte et formes, Marp n'est pas le bon outil pour cette étape.</p>
      <p><strong>Marp est-il gratuit ?</strong> Oui. Marp est open source sous licence MIT, donc libre d'utilisation, auto-hébergeable et modifiable.</p>
      <p><strong>Marp vs reveal.js vs Slidev – lequel choisir ?</strong> Choisissez Marp pour les diaporamas en Markdown pur les plus simples et un export PDF/pptx facile, reveal.js pour un framework de diapositives web mature, HTML-first et profondément personnalisable, et Slidev pour du Markdown accompagné de composants Vue interactifs. Les trois sont sous licence MIT ; aucun n'exporte du PowerPoint directement modifiable.</p>
      <h2>À retenir</h2>
      <p>Marp est la manière la plus nette de transformer en diapositives le Markdown que vous avez déjà – un seul fichier <code>.md</code>, des thèmes CSS et une CLI qui exporte HTML, PDF et PowerPoint. Rappelez-vous simplement la seule limite honnête : le pptx sort sous forme d'images, alors traitez-le comme un format de livraison plutôt que comme quelque chose que vous continuerez à éditer dans PowerPoint. Si votre besoin est « j'ai le Markdown, donnez-moi un diaporama », Marp est difficile à battre. Quand le diaporama doit être généré, rester conforme à la marque et demeurer modifiable au sein d'un flux de conception plus vaste, c'est là qu'un <a href="/">Agent-Native Design Workspace</a> prend le relais.</p>
  ru:
    title: 'Marp: инструмент для презентаций на Markdown (и как экспортировать в PowerPoint)'
    summary: 'Marp — это открытая экосистема презентаций на Markdown: пишите слайды в Markdown, а затем экспортируйте их в HTML, PDF или PowerPoint с помощью Marp CLI. В этом руководстве рассматриваются темы Marp, создание слайдов в Markdown, экспорт в pptx (в виде изображений) через marp-cli, сравнение с reveal.js и Slidev, а также то, где уместен Agent-Native Design Workspace.'
    category: 'Инструменты и навыки'
    bodyHtml: |
      <p>Если вы искали «marp» в надежде на стоматологический или ортодонтический термин, то это другое: <strong>Marp — это Markdown Presentation Ecosystem</strong>, открытый (MIT) проект команды <code>marp-team</code> на GitHub для создания слайд-презентаций путём написания обычного Markdown. Никакого перетаскивания блоков по холсту, никакого проприетарного формата файлов. Вы пишете <code># Slide title</code> и несколько пунктов, добавляете одну-две директивы, и Marp превращает это в презентацию. (Не связанный с этим «MARPE», который вам мог попадаться, — это процедура расширения верхней челюсти в ортодонтии, к слайдам она не имеет отношения. Эта статья исключительно о Marp, инструменте для презентаций.)</p>
      <p>Среди открытых инструментов для презентаций Marp — самый простой и максимально «родной» для Markdown вариант, и именно поэтому разработчики к нему тянутся. Это честное руководство о том, что такое Marp, как работают написание и темы, как Marp CLI экспортирует в PowerPoint и PDF (и об одной оговорке, которая застаёт людей врасплох), как он держится против reveal.js и Slidev, и где подхватывает дизайн-воркспейс, когда Markdown упирается в потолок.</p>
      <h2>Что такое Marp</h2>
      <p>Marp — это небольшая экосистема инструментов вокруг одной идеи: <strong>слайды как Markdown</strong>. У неё три подвижные части. <strong>Marpit</strong> — низкоуровневый фреймворк, который превращает Markdown вместе с темой в HTML и CSS слайдов. <strong>Marp Core</strong> — готовый к работе движок поверх Marpit, с темой по умолчанию, обработкой изображений и поддержкой формул. <strong>Marp CLI</strong> (<code>marp-team/marp-cli</code>) — инструмент командной строки, который рендерит ваш Markdown-файл в HTML, PDF или PowerPoint. Есть и официальное <strong>расширение для VS Code</strong>, дающее живой предпросмотр бок о бок прямо во время набора.</p>
      <p>Вы пишете в одном файле <code>.md</code>. Слайды разделяются <code>---</code>, настройки для отдельных слайдов и глобальные задаются через директивы Marp (например, <code>theme:</code>, <code>paginate:</code> или <code>class:</code>), и всё остаётся простым текстом под контролем версий. В этом вся привлекательность: ваша презентация чисто различается в Git, лежит рядом с кодом и никогда не запирает вас в бинарный формат.</p>
      <h2>Темы Marp и написание в Markdown</h2>
      <p>Написание презентации Marp намеренно минималистично. Блок front-matter в начале файла задаёт глобальные директивы, <code>---</code> разбивает слайды, а стандартный Markdown — заголовки, списки, блоки кода, изображения, таблицы — становится вашим контентом. Поскольку Marp под капотом рендерит в HTML и CSS, всё, что выражает Markdown, отображается на слайд без дополнительного синтаксиса, который нужно учить.</p>
      <p><strong>Темы Marp</strong> — это просто CSS. Marp Core поставляется с несколькими встроенными темами (<code>default</code>, <code>gaia</code>, <code>uncover</code>), и переключаетесь между ними вы одной директивой <code>theme:</code>. Когда встроенных не хватает, вы пишете собственную тему как CSS-файл — нацеливаетесь на классы, которые предоставляет Marpit, регистрируете её в CLI через <code>--theme</code> или <code>--theme-set</code>, и каждый слайд её подхватывает. Для точечных правок вы можете вставить в отдельный слайд блок <code>&lt;style&gt;</code> с ограниченной областью действия. Эта CSS-нативная модель — одновременно потолок и пол Marp: если вы уверенно владеете CSS, у вас полный контроль, а если нет — стандартные настройки всё равно выглядят опрятно.</p>
      <h2>Экспорт Marp в PowerPoint и PDF</h2>
      <p>Именно на экспорте Marp CLI отрабатывает своё место. Укажите ей на ваш Markdown и выберите цель:</p>
      <ul><li><strong>HTML</strong> — <code>marp deck.md -o deck.html</code> создаёт самодостаточную веб-презентацию, которую вы показываете в браузере.</li><li><strong>PDF</strong> — <code>marp deck.md --pdf</code> рендерит готовый к печати PDF, идеальный для раздаточных материалов и обмена.</li><li><strong>PowerPoint</strong> — <code>marp deck.md --pptx</code> экспортирует файл <code>.pptx</code>, который можно открыть в PowerPoint или Keynote.</li></ul>
      <p>Одна оговорка, о которой стоит сказать прямо: <strong>экспорт Marp в PPTX рендерит каждый слайд как изображение</strong>, а не как нативные, редактируемые фигуры PowerPoint. Каждый слайд становится полноформатной картинкой внутри оболочки <code>.pptx</code>. Это идеально, когда нужно передать файл тому, кто живёт в PowerPoint и должен лишь показать его, — но после экспорта вы не сможете щёлкнуть и отредактировать текстовое поле, перекрасить фигуру или подвинуть пункт. Если вашему процессу нужно продолжать редактирование в PowerPoint потом, относитесь к pptx от Marp как к формату доставки, а не исходному формату. (Экспериментальные «редактируемые» режимы экспорта появлялись и исчезали; надёжное, задокументированное поведение — одно изображение на слайд.)</p>
      <h2>Marp против Slidev против reveal.js</h2>
      <p>Marp — не единственный инструмент для презентаций с приоритетом Markdown. Вот честное сравнение по осям, которые решают ваш выбор:</p>
      <table><thead><tr><th>Инструмент</th><th>Написание</th><th>Редактируемый экспорт PPTX</th><th>Шаблонизация</th><th>Лицензия</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>Чистый Markdown + директивы</td><td>pptx как изображения (не редактируемые фигуры)</td><td>CSS-темы</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML (плагин Markdown опционально)</td><td>Нет нативного pptx</td><td>HTML/CSS-темы</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + компоненты Vue</td><td>Нет нативного pptx (PDF/PNG/SPA)</td><td>Темы Vue + Windi/UnoCSS</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>Опишите это своему агенту</td><td>Редактируемая презентация на основе дизайн-системы</td><td>Дизайн-система + библиотека шаблонов</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> — выбор, когда вам нужно меньше всего церемоний: один Markdown-файл и CLI. <strong><a href="/blog/reveal-js/">reveal.js</a></strong> — зрелый фреймворк веб-слайдов: HTML-first, бесконечно настраиваемый и с самым насыщенным опытом в браузере, но вы пишете HTML, и нативного пути в PowerPoint нет. <strong><a href="/blog/slidev/">Slidev</a></strong> находится посередине — для разработчиков, которым нужен Markdown плюс настоящие компоненты Vue, подсветка кода и интерактивные вставки: мощнее Marp, но и настройки больше. Все три под лицензией MIT и не экспортируют ничего напрямую редактируемого в PowerPoint.</p>
      <h2>Где уместен Open Design</h2>
      <p>Marp превосходно делает ровно одну вещь: берёт уже написанный вами Markdown и превращает в слайды. Но он требует, чтобы презентацию писали <em>вы</em> — структуру, формулировки, CSS темы. Он не генерирует контент, не удерживает его в рамках бренда между проектами и не даёт редактируемый артефакт после экспорта.</p>
      <p><a href="/">Open Design</a> — это слой над ним. Это открытый (Apache-2.0), local-first, работающий с вашими собственными ключами <strong>Agent-Native Design Workspace</strong>, который находится вне того агента для кода, что вы уже используете. Вместо того чтобы писать Markdown вручную, вы описываете нужную презентацию, и агент генерирует редактируемую на основе дизайн-системы — с фирменными HTML-шаблонами презентаций в его <a href="/plugins/templates/">библиотеке плагинов</a> как отправной точкой. Это не замена Marp, где вы пишете Markdown: когда контент уже есть в Markdown и нужна быстрая презентация из командной строки, Marp — правильный инструмент. Когда вы хотите, чтобы презентация была сгенерирована, соответствовала бренду и оставалась редактируемой как часть более крупного дизайн-процесса, — это задача воркспейса. Если вы уже делаете слайды с агентом для кода, посмотрите, <a href="/blog/claude-ppt-skills/">как агент для кода создаёт слайд-презентации</a>.</p>
      <h2>Часто задаваемые вопросы</h2>
      <p><strong>Что такое Marp?</strong> Marp — это открытая Markdown Presentation Ecosystem от команды <code>marp-team</code> на GitHub. Вы пишете слайды в Markdown, а Marp CLI экспортирует их в HTML, PDF или PowerPoint. Он под лицензией MIT и включает Marp Core, фреймворк Marpit и расширение для VS Code. К ортодонтическому термину «MARPE» он отношения не имеет.</p>
      <p><strong>Как экспортировать презентацию Marp в PowerPoint?</strong> Установите Marp CLI (<code>marp-cli</code>) и выполните <code>marp deck.md --pptx</code>. Учтите, что экспортированный <code>.pptx</code> рендерит каждый слайд как изображение, а не как нативные редактируемые фигуры PowerPoint — отлично для передачи, но потом вы не сможете редактировать текст или фигуры внутри PowerPoint.</p>
      <p><strong>Может ли Marp экспортировать редактируемые файлы PowerPoint?</strong> Надёжно — нет. Задокументированное поведение Marp для PPTX — одно изображение на слайд. Если вам нужен нативно редактируемый <code>.pptx</code> с настоящими текстовыми полями и фигурами, для этого шага Marp — неподходящий инструмент.</p>
      <p><strong>Marp бесплатен?</strong> Да. Marp — это открытый исходный код под лицензией MIT, поэтому им можно свободно пользоваться, размещать у себя и изменять.</p>
      <p><strong>Marp против reveal.js против Slidev — что выбрать?</strong> Выбирайте Marp для самых простых презентаций на чистом Markdown и лёгкого экспорта в PDF/pptx, reveal.js — для зрелого HTML-first фреймворка веб-слайдов с глубокой настройкой, а Slidev — для Markdown вместе с интерактивными компонентами Vue. Все три под лицензией MIT; ни один не экспортирует напрямую редактируемый PowerPoint.</p>
      <h2>Вывод</h2>
      <p>Marp — это самый чистый способ превратить уже имеющийся у вас Markdown в слайды: один файл <code>.md</code>, CSS-темы и CLI, который экспортирует в HTML, PDF и PowerPoint. Просто помните одно честное ограничение: pptx выходит в виде изображений, поэтому относитесь к нему как к формату доставки, а не к тому, что вы будете дальше редактировать в PowerPoint. Если ваша потребность — «у меня есть Markdown, дайте презентацию», Marp трудно превзойти. А когда презентацию нужно сгенерировать, удержать в рамках бренда и оставить редактируемой внутри более крупного дизайн-процесса, — вот тут подхватывает <a href="/">Agent-Native Design Workspace</a>.</p>
  es:
    title: 'Marp: la herramienta de presentaciones en Markdown (y cómo exportar a PowerPoint)'
    summary: 'Marp es el ecosistema de presentaciones en Markdown de código abierto: escribe las diapositivas en Markdown y luego expórtalas a HTML, PDF o PowerPoint con la Marp CLI. Esta guía cubre los temas de Marp, la escritura en Markdown, la exportación a pptx (como imágenes) con marp-cli, cómo se compara con reveal.js y Slidev, y dónde encaja un Agent-Native Design Workspace.'
    category: 'Herramientas y skills'
    bodyHtml: |
      <p>Si buscaste «marp» esperando un término dental u ortodóncico, este es el otro: <strong>Marp es el Markdown Presentation Ecosystem</strong>, un proyecto de código abierto (MIT) del equipo <code>marp-team</code> en GitHub para crear presentaciones escribiendo Markdown puro. Sin arrastrar cajas por un lienzo, sin formato de archivo propietario. Escribes <code># Slide title</code> y unas viñetas, añades una directiva o dos, y Marp lo convierte en una presentación. (El «MARPE» sin relación que quizá hayas visto es un procedimiento de expansión maxilar en ortodoncia, nada que ver con las diapositivas. Este artículo trata estrictamente de Marp, la herramienta de presentaciones.)</p>
      <p>Marp es la opción más sencilla y más nativa de Markdown entre las herramientas de presentaciones de código abierto, y por eso mismo los desarrolladores recurren a ella. Esta es una guía honesta sobre qué es Marp, cómo funcionan la escritura y los temas, cómo la Marp CLI exporta a PowerPoint y PDF (y la única salvedad que pilla a la gente por sorpresa), cómo se compara con reveal.js y Slidev, y dónde toma el relevo un design workspace cuando Markdown se queda corto.</p>
      <h2>Qué es Marp</h2>
      <p>Marp es un pequeño ecosistema de herramientas construido en torno a una idea: <strong>diapositivas como Markdown</strong>. Tiene tres piezas móviles. <strong>Marpit</strong> es el framework de bajo nivel que convierte Markdown más un tema en el HTML y CSS de las diapositivas. <strong>Marp Core</strong> es el motor con todo incluido que se apoya en Marpit, con un tema por defecto, gestión de imágenes y soporte de fórmulas. <strong>Marp CLI</strong> (<code>marp-team/marp-cli</code>) es la herramienta de línea de comandos que renderiza tu archivo Markdown a HTML, PDF o PowerPoint. También existe una <strong>extensión oficial de VS Code</strong> que te ofrece una vista previa lado a lado en tiempo real mientras escribes.</p>
      <p>Escribes en un único archivo <code>.md</code>. Las diapositivas se separan con <code>---</code>, los ajustes por diapositiva y globales se establecen mediante directivas de Marp (como <code>theme:</code>, <code>paginate:</code> o <code>class:</code>), y todo permanece como texto plano versionable. En eso reside todo su atractivo: tu presentación hace diff limpio en Git, vive junto a tu código y nunca te encierra en un formato binario.</p>
      <h2>Temas de Marp y escritura en Markdown</h2>
      <p>Escribir una presentación de Marp es deliberadamente minimalista. Un bloque de front-matter al inicio del archivo fija las directivas globales, <code>---</code> separa las diapositivas, y el Markdown estándar —encabezados, listas, bloques de código, imágenes, tablas— se convierte en tu contenido. Como Marp renderiza a HTML y CSS bajo el capó, todo lo que Markdown expresa se traduce en una diapositiva sin sintaxis adicional que aprender.</p>
      <p><strong>Los temas de Marp</strong> son simplemente CSS. Marp Core incluye un puñado de temas integrados (<code>default</code>, <code>gaia</code>, <code>uncover</code>), y cambias entre ellos con una sola directiva <code>theme:</code>. Cuando los integrados no bastan, escribes tu propio tema como archivo CSS: apunta a las clases que expone Marpit, regístralo en la CLI mediante <code>--theme</code> o <code>--theme-set</code>, y cada diapositiva lo adopta. Para ajustes en línea puedes colocar un bloque <code>&lt;style&gt;</code> con alcance limitado en una sola diapositiva. Este modelo nativo de CSS es a la vez el techo y el suelo de Marp: si te manejas con CSS tienes control total, y si no, los valores por defecto igual se ven pulcros.</p>
      <h2>Exportar Marp a PowerPoint y PDF</h2>
      <p>La exportación es donde la Marp CLI se gana su lugar. Apúntala a tu Markdown y elige un destino:</p>
      <ul><li><strong>HTML</strong> — <code>marp deck.md -o deck.html</code> produce una presentación web autónoma que expones en el navegador.</li><li><strong>PDF</strong> — <code>marp deck.md --pdf</code> renderiza un PDF listo para imprimir, ideal para materiales de apoyo y para compartir.</li><li><strong>PowerPoint</strong> — <code>marp deck.md --pptx</code> exporta un archivo <code>.pptx</code> que puedes abrir en PowerPoint o Keynote.</li></ul>
      <p>La única salvedad que conviene decir sin rodeos: <strong>la exportación PPTX de Marp renderiza cada diapositiva como una imagen</strong>, no como formas nativas y editables de PowerPoint. Cada diapositiva se convierte en una imagen a sangre dentro del envoltorio <code>.pptx</code>. Es perfecto cuando necesitas entregar un archivo a alguien que vive en PowerPoint y solo tiene que presentarlo, pero, una vez exportado, no puedes hacer clic para editar un cuadro de texto, recolorear una forma ni mover una viñeta. Si tu flujo de trabajo necesita seguir editando en PowerPoint después, trata el pptx de Marp como un formato de entrega, no como un formato fuente. (Han aparecido y desaparecido modos de exportación «editable» experimentales; el comportamiento fiable y documentado es una imagen por diapositiva.)</p>
      <h2>Marp vs Slidev vs reveal.js</h2>
      <p>Marp no es la única herramienta de presentaciones con enfoque Markdown-first. Aquí va la comparación honesta según los ejes que deciden tu elección:</p>
      <table><thead><tr><th>Herramienta</th><th>Escritura</th><th>Exportación PPTX editable</th><th>Plantillas</th><th>Licencia</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>Markdown puro + directivas</td><td>pptx como imágenes (no formas editables)</td><td>Temas CSS</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML (plugin de Markdown opcional)</td><td>Sin pptx nativo</td><td>Temas HTML/CSS</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + componentes Vue</td><td>Sin pptx nativo (PDF/PNG/SPA)</td><td>Temas Vue + Windi/UnoCSS</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>Descríbeselo a tu agente</td><td>Presentación editable sobre un design system</td><td>Design system + biblioteca de plantillas</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> es la elección cuando quieres la menor ceremonia posible: un único archivo Markdown y una CLI. <strong><a href="/blog/reveal-js/">reveal.js</a></strong> es el maduro framework de diapositivas web: HTML-first, personalizable sin fin y con la experiencia más rica en el navegador, pero escribes HTML y no hay una vía nativa a PowerPoint. <strong><a href="/blog/slidev/">Slidev</a></strong> se sitúa en medio, para desarrolladores que quieren Markdown más componentes Vue reales, resaltado de código e integraciones interactivas: más potencia que Marp, pero también más configuración. Las tres están bajo licencia MIT y ninguna exporta nada directamente editable en PowerPoint.</p>
      <h2>Dónde encaja Open Design</h2>
      <p>Marp destaca exactamente en una cosa: tomar el Markdown que ya has escrito y convertirlo en diapositivas. Pero te pide a <em>ti</em> que escribas la presentación: la estructura, la redacción, el CSS del tema. No genera el contenido, no lo mantiene fiel a la marca entre proyectos ni te da un artefacto editable una vez exportado.</p>
      <p><a href="/">Open Design</a> es la capa por encima de eso. Es un <strong>Agent-Native Design Workspace</strong> de código abierto (Apache-2.0), local-first y con tus propias claves, que se sitúa fuera del agente de código que ya usas. En lugar de escribir Markdown a mano, describes la presentación que quieres y el agente genera una editable sobre un design system, con plantillas propias de presentaciones HTML en su <a href="/plugins/templates/">biblioteca de plugins</a> como punto de partida. No es un reemplazo del Marp escrito en Markdown: cuando ya tienes el contenido en Markdown y quieres una presentación rápida desde la línea de comandos, Marp es la herramienta adecuada. Cuando quieres que la presentación se genere, se mantenga fiel a la marca y siga siendo editable como parte de un flujo de diseño mayor, ese es el trabajo del workspace. Si ya haces diapositivas con un agente de código, mira <a href="/blog/claude-ppt-skills/">cómo un agente de código crea presentaciones</a>.</p>
      <h2>Preguntas frecuentes</h2>
      <p><strong>¿Qué es Marp?</strong> Marp es el Markdown Presentation Ecosystem de código abierto del equipo <code>marp-team</code> en GitHub. Escribes diapositivas en Markdown y la Marp CLI las exporta a HTML, PDF o PowerPoint. Está bajo licencia MIT e incluye Marp Core, el framework Marpit y una extensión de VS Code. No tiene relación con el término ortodóncico «MARPE».</p>
      <p><strong>¿Cómo exporto una presentación de Marp a PowerPoint?</strong> Instala la Marp CLI (<code>marp-cli</code>) y ejecuta <code>marp deck.md --pptx</code>. Ten en cuenta que el <code>.pptx</code> exportado renderiza cada diapositiva como una imagen, no como formas nativas y editables de PowerPoint: es genial para la entrega, pero después no podrás editar el texto ni las formas dentro de PowerPoint.</p>
      <p><strong>¿Puede Marp exportar archivos de PowerPoint editables?</strong> No de forma fiable. El comportamiento PPTX documentado de Marp es una imagen por diapositiva. Si necesitas un <code>.pptx</code> nativamente editable con cuadros de texto y formas reales, Marp es la herramienta equivocada para ese paso.</p>
      <p><strong>¿Marp es gratis?</strong> Sí. Marp es de código abierto bajo la licencia MIT, así que es libre de usar, autoalojar y modificar.</p>
      <p><strong>Marp vs reveal.js vs Slidev, ¿cuál debería usar?</strong> Elige Marp para las presentaciones en Markdown puro más sencillas y una exportación fácil a PDF/pptx, reveal.js para un maduro framework de diapositivas web con enfoque HTML-first y personalización profunda, y Slidev para Markdown más componentes Vue interactivos. Las tres están bajo licencia MIT; ninguna exporta PowerPoint directamente editable.</p>
      <h2>La conclusión</h2>
      <p>Marp es la forma más limpia de convertir en diapositivas el Markdown que ya tienes: un único archivo <code>.md</code>, temas CSS y una CLI que exporta HTML, PDF y PowerPoint. Solo recuerda la única limitación honesta: el pptx sale como imágenes, así que trátalo como un formato de entrega y no como algo que seguirás editando en PowerPoint. Si tu necesidad es «tengo el Markdown, dame una presentación», es difícil superar a Marp. Cuando la presentación deba generarse, mantenerse fiel a la marca y seguir siendo editable dentro de un flujo de diseño mayor, ahí es donde toma el relevo un <a href="/">Agent-Native Design Workspace</a>.</p>
  pt-br:
    title: 'Marp: a ferramenta de apresentações em Markdown (e como exportar para PowerPoint)'
    summary: 'O Marp é o ecossistema open source de apresentações em Markdown: escreva os slides em Markdown e depois exporte para HTML, PDF ou PowerPoint com a Marp CLI. Este guia aborda os temas do Marp, a escrita em Markdown, a exportação para pptx (como imagens) com o marp-cli, como ele se compara ao reveal.js e ao Slidev, e onde um Agent-Native Design Workspace entra em cena.'
    category: 'Ferramentas e skills'
    bodyHtml: |
      <p>Se você pesquisou «marp» esperando um termo odontológico ou ortodôntico, este é o outro: <strong>o Marp é o Markdown Presentation Ecosystem</strong> — um projeto open source (MIT) da equipe <code>marp-team</code> no GitHub para criar apresentações escrevendo Markdown puro. Sem arrastar caixas por um canvas, sem formato de arquivo proprietário. Você escreve <code># Slide title</code> e alguns tópicos, adiciona uma diretiva ou duas, e o Marp transforma isso em uma apresentação. (O «MARPE» sem relação que você pode ter visto é um procedimento de expansão maxilar na ortodontia — nada a ver com slides. Este artigo trata estritamente do Marp, a ferramenta de apresentações.)</p>
      <p>O Marp é a opção mais simples e mais nativa de Markdown entre as ferramentas de apresentação open source, e é exatamente por isso que os desenvolvedores recorrem a ela. Este é um guia honesto sobre o que é o Marp, como funcionam a escrita e os temas, como a Marp CLI exporta para PowerPoint e PDF (e a única ressalva que pega as pessoas de surpresa), como ele se compara ao reveal.js e ao Slidev, e onde um design workspace assume quando o Markdown chega ao limite.</p>
      <h2>O que é o Marp</h2>
      <p>O Marp é um pequeno ecossistema de ferramentas construído em torno de uma ideia: <strong>slides como Markdown</strong>. Ele tem três partes móveis. O <strong>Marpit</strong> é o framework de baixo nível que transforma Markdown mais um tema no HTML e CSS dos slides. O <strong>Marp Core</strong> é o motor pronto para uso sobre o Marpit, com um tema padrão, tratamento de imagens e suporte a fórmulas. A <strong>Marp CLI</strong> (<code>marp-team/marp-cli</code>) é a ferramenta de linha de comando que renderiza seu arquivo Markdown em HTML, PDF ou PowerPoint. Há também uma <strong>extensão oficial do VS Code</strong> que oferece uma prévia lado a lado em tempo real enquanto você digita.</p>
      <p>Você escreve em um único arquivo <code>.md</code>. Os slides são separados por <code>---</code>, as configurações por slide e globais são definidas por diretivas do Marp (como <code>theme:</code>, <code>paginate:</code> ou <code>class:</code>), e tudo permanece como texto puro versionável. É aí que está todo o apelo: sua apresentação faz diff de forma limpa no Git, fica ao lado do seu código e nunca prende você a um formato binário.</p>
      <h2>Temas do Marp e escrita em Markdown</h2>
      <p>Escrever uma apresentação no Marp é deliberadamente minimalista. Um bloco de front-matter no topo do arquivo define as diretivas globais, <code>---</code> quebra os slides, e o Markdown padrão — títulos, listas, blocos de código, imagens, tabelas — vira o seu conteúdo. Como o Marp renderiza para HTML e CSS por baixo dos panos, tudo o que o Markdown expressa se mapeia para um slide sem sintaxe extra a aprender.</p>
      <p><strong>Os temas do Marp</strong> são apenas CSS. O Marp Core traz um punhado de temas integrados (<code>default</code>, <code>gaia</code>, <code>uncover</code>), e você alterna entre eles com uma única diretiva <code>theme:</code>. Quando os integrados não bastam, você escreve seu próprio tema como um arquivo CSS — mira nas classes que o Marpit expõe, registra-o na CLI via <code>--theme</code> ou <code>--theme-set</code>, e cada slide o adota. Para ajustes inline, você pode inserir um bloco <code>&lt;style&gt;</code> com escopo em um único slide. Esse modelo nativo de CSS é ao mesmo tempo o teto e o piso do Marp: se você é à vontade com CSS, tem controle total, e se não é, os padrões continuam com aparência limpa.</p>
      <h2>Exportando o Marp para PowerPoint e PDF</h2>
      <p>É na exportação que a Marp CLI justifica seu lugar. Aponte-a para o seu Markdown e escolha um destino:</p>
      <ul><li><strong>HTML</strong> — <code>marp deck.md -o deck.html</code> produz uma apresentação web autossuficiente que você exibe no navegador.</li><li><strong>PDF</strong> — <code>marp deck.md --pdf</code> renderiza um PDF pronto para impressão, ideal para materiais de apoio e compartilhamento.</li><li><strong>PowerPoint</strong> — <code>marp deck.md --pptx</code> exporta um arquivo <code>.pptx</code> que você pode abrir no PowerPoint ou no Keynote.</li></ul>
      <p>A única ressalva que vale dizer sem rodeios: <strong>a exportação PPTX do Marp renderiza cada slide como uma imagem</strong>, não como formas nativas e editáveis do PowerPoint. Cada slide vira uma imagem de sangria total dentro do invólucro <code>.pptx</code>. Isso é perfeito quando você precisa entregar um arquivo a alguém que vive no PowerPoint e só precisa apresentá-lo — mas, uma vez exportado, você não consegue clicar para editar uma caixa de texto, recolorir uma forma ou mover um tópico. Se o seu fluxo precisar continuar editando no PowerPoint depois, trate o pptx do Marp como um formato de entrega, não um formato de origem. (Modos de exportação «editável» experimentais surgiram e sumiram; o comportamento confiável e documentado é uma imagem por slide.)</p>
      <h2>Marp vs Slidev vs reveal.js</h2>
      <p>O Marp não é a única ferramenta de apresentação com abordagem Markdown-first. Aqui vai a comparação honesta pelos eixos que decidem a sua escolha:</p>
      <table><thead><tr><th>Ferramenta</th><th>Escrita</th><th>Exportação PPTX editável</th><th>Templating</th><th>Licença</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>Markdown puro + diretivas</td><td>pptx como imagens (não são formas editáveis)</td><td>Temas CSS</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML (plugin de Markdown opcional)</td><td>Sem pptx nativo</td><td>Temas HTML/CSS</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + componentes Vue</td><td>Sem pptx nativo (PDF/PNG/SPA)</td><td>Temas Vue + Windi/UnoCSS</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>Descreva para o seu agente</td><td>Apresentação editável sobre um design system</td><td>Design system + biblioteca de templates</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> é a escolha quando você quer o mínimo de cerimônia — um único arquivo Markdown e uma CLI. <strong><a href="/blog/reveal-js/">reveal.js</a></strong> é o maduro framework de slides web: HTML-first, personalizável sem fim e com a experiência mais rica no navegador, mas você escreve HTML e não há um caminho nativo para o PowerPoint. <strong><a href="/blog/slidev/">Slidev</a></strong> fica no meio, para desenvolvedores que querem Markdown mais componentes Vue de verdade, realce de código e embeds interativos — mais poder que o Marp, mas também mais configuração. Os três têm licença MIT e nenhum exporta nada diretamente editável no PowerPoint.</p>
      <h2>Onde o Open Design entra</h2>
      <p>O Marp é excelente em exatamente uma coisa: pegar o Markdown que você já escreveu e transformá-lo em slides. Mas ele pede que <em>você</em> escreva a apresentação — a estrutura, a redação, o CSS do tema. Ele não gera o conteúdo, não o mantém fiel à marca entre projetos, nem lhe dá um artefato editável depois de exportado.</p>
      <p><a href="/">Open Design</a> é a camada acima disso. É um <strong>Agent-Native Design Workspace</strong> open source (Apache-2.0), local-first e com suas próprias chaves, que fica fora do agente de código que você já usa. Em vez de escrever Markdown à mão, você descreve a apresentação que quer, e o agente gera uma editável sobre um design system — com templates próprios de apresentações HTML na sua <a href="/plugins/templates/">biblioteca de plugins</a> como ponto de partida. Não é um substituto para o Marp escrito em Markdown: quando você já tem o conteúdo em Markdown e quer uma apresentação rápida pela linha de comando, o Marp é a ferramenta certa. Quando você quer a apresentação gerada, fiel à marca e ainda editável como parte de um fluxo de design maior, esse é o trabalho do workspace. Se você já faz slides com um agente de código, veja <a href="/blog/claude-ppt-skills/">como um agente de código cria apresentações</a>.</p>
      <h2>Perguntas frequentes</h2>
      <p><strong>O que é o Marp?</strong> O Marp é o Markdown Presentation Ecosystem open source da equipe <code>marp-team</code> no GitHub. Você escreve slides em Markdown, e a Marp CLI os exporta para HTML, PDF ou PowerPoint. Tem licença MIT e inclui o Marp Core, o framework Marpit e uma extensão do VS Code. Não tem relação com o termo ortodôntico «MARPE».</p>
      <p><strong>Como exporto uma apresentação do Marp para o PowerPoint?</strong> Instale a Marp CLI (<code>marp-cli</code>) e execute <code>marp deck.md --pptx</code>. Observe que o <code>.pptx</code> exportado renderiza cada slide como uma imagem, não como formas nativas e editáveis do PowerPoint — ótimo para repasse, mas você não conseguirá editar o texto ou as formas dentro do PowerPoint depois.</p>
      <p><strong>O Marp pode exportar arquivos editáveis do PowerPoint?</strong> Não de forma confiável. O comportamento PPTX documentado do Marp é uma imagem por slide. Se você precisa de um <code>.pptx</code> nativamente editável, com caixas de texto e formas de verdade, o Marp é a ferramenta errada para essa etapa.</p>
      <p><strong>O Marp é gratuito?</strong> Sim. O Marp é open source sob a licença MIT, então é livre para usar, hospedar por conta própria e modificar.</p>
      <p><strong>Marp vs reveal.js vs Slidev — qual devo usar?</strong> Escolha o Marp para as apresentações em Markdown puro mais simples e uma exportação fácil para PDF/pptx, o reveal.js para um maduro framework de slides web com abordagem HTML-first e personalização profunda, e o Slidev para Markdown mais componentes Vue interativos. Os três têm licença MIT; nenhum exporta PowerPoint diretamente editável.</p>
      <h2>A conclusão</h2>
      <p>O Marp é a maneira mais limpa de transformar em slides o Markdown que você já tem — um único arquivo <code>.md</code>, temas CSS e uma CLI que exporta HTML, PDF e PowerPoint. Só lembre da única limitação honesta: o pptx sai como imagens, então trate-o como um formato de entrega, e não como algo que você continuará editando no PowerPoint. Se a sua necessidade é «tenho o Markdown, me dê uma apresentação», o Marp é difícil de superar. Quando a apresentação precisa ser gerada, permanecer fiel à marca e continuar editável dentro de um fluxo de design maior, é aí que um <a href="/">Agent-Native Design Workspace</a> assume.</p>
  it:
    title: 'Marp: lo strumento per presentazioni in Markdown (e come esportare in PowerPoint)'
    summary: 'Marp è l''ecosistema open source per presentazioni in Markdown: scrivi le slide in Markdown e poi esportale in HTML, PDF o PowerPoint con la Marp CLI. Questa guida illustra i temi di Marp, la scrittura in Markdown, l''esportazione in pptx (come immagini) con marp-cli, il confronto con reveal.js e Slidev, e dove si inserisce un Agent-Native Design Workspace.'
    category: 'Strumenti e skill'
    bodyHtml: |
      <p>Se hai cercato «marp» aspettandoti un termine odontoiatrico o ortodontico, questo è l'altro: <strong>Marp è il Markdown Presentation Ecosystem</strong>, un progetto open source (MIT) del team <code>marp-team</code> su GitHub per costruire presentazioni scrivendo puro Markdown. Niente riquadri da trascinare su una tela, nessun formato di file proprietario. Scrivi <code># Slide title</code> e qualche punto elenco, aggiungi una direttiva o due, e Marp lo trasforma in una presentazione. (Il «MARPE» senza alcun legame che potresti aver visto è una procedura di espansione mascellare in ortodonzia, niente a che vedere con le slide. Questo articolo riguarda esclusivamente Marp, lo strumento per presentazioni.)</p>
      <p>Marp è l'opzione più semplice e più nativa di Markdown tra gli strumenti di presentazione open source, ed è proprio per questo che gli sviluppatori vi ricorrono. Questa è una guida onesta su cosa sia Marp, su come funzionano la scrittura e i temi, su come la Marp CLI esporta verso PowerPoint e PDF (e sull'unica avvertenza che coglie tutti di sorpresa), su come si confronta con reveal.js e Slidev, e su dove subentra un design workspace quando Markdown arriva al capolinea.</p>
      <h2>Cos'è Marp</h2>
      <p>Marp è un piccolo ecosistema di strumenti costruito attorno a un'unica idea: <strong>le slide come Markdown</strong>. Ha tre parti in movimento. <strong>Marpit</strong> è il framework di basso livello che trasforma il Markdown più un tema in HTML e CSS delle slide. <strong>Marp Core</strong> è il motore pronto all'uso poggiato su Marpit, con un tema predefinito, gestione delle immagini e supporto alle formule. <strong>Marp CLI</strong> (<code>marp-team/marp-cli</code>) è lo strumento da riga di comando che renderizza il tuo file Markdown in HTML, PDF o PowerPoint. C'è anche un'<strong>estensione ufficiale per VS Code</strong> che ti offre un'anteprima affiancata in tempo reale mentre scrivi.</p>
      <p>Scrivi in un unico file <code>.md</code>. Le slide sono separate da <code>---</code>, le impostazioni per singola slide e globali si definiscono tramite direttive Marp (come <code>theme:</code>, <code>paginate:</code> o <code>class:</code>), e tutto resta testo semplice versionabile. È tutto qui il fascino: la tua presentazione fa il diff in modo pulito in Git, vive accanto al tuo codice e non ti vincola mai a un formato binario.</p>
      <h2>I temi di Marp e la scrittura in Markdown</h2>
      <p>Scrivere una presentazione Marp è volutamente minimale. Un blocco di front-matter in cima al file imposta le direttive globali, <code>---</code> divide le slide, e il Markdown standard — titoli, elenchi, blocchi di codice, immagini, tabelle — diventa il tuo contenuto. Poiché Marp renderizza in HTML e CSS dietro le quinte, tutto ciò che il Markdown esprime si traduce in una slide senza sintassi aggiuntiva da imparare.</p>
      <p><strong>I temi di Marp</strong> sono semplicemente CSS. Marp Core include una manciata di temi integrati (<code>default</code>, <code>gaia</code>, <code>uncover</code>), e passi dall'uno all'altro con una singola direttiva <code>theme:</code>. Quando quelli integrati non bastano, scrivi il tuo tema come file CSS: prendi di mira le classi che Marpit espone, registralo nella CLI tramite <code>--theme</code> o <code>--theme-set</code>, e ogni slide lo adotta. Per ritocchi inline puoi inserire un blocco <code>&lt;style&gt;</code> con ambito limitato in una singola slide. Questo modello CSS-nativo è al tempo stesso il soffitto e il pavimento di Marp: se hai dimestichezza con il CSS hai pieno controllo, e se non ce l'hai, i valori predefiniti restano comunque puliti.</p>
      <h2>Esportare Marp in PowerPoint e PDF</h2>
      <p>È nell'esportazione che la Marp CLI si guadagna il suo posto. Puntala sul tuo Markdown e scegli una destinazione:</p>
      <ul><li><strong>HTML</strong> — <code>marp deck.md -o deck.html</code> produce una presentazione web autonoma che esponi nel browser.</li><li><strong>PDF</strong> — <code>marp deck.md --pdf</code> renderizza un PDF pronto per la stampa, ideale per dispense e condivisione.</li><li><strong>PowerPoint</strong> — <code>marp deck.md --pptx</code> esporta un file <code>.pptx</code> che puoi aprire in PowerPoint o Keynote.</li></ul>
      <p>L'unica avvertenza che vale la pena dire chiaramente: <strong>l'esportazione PPTX di Marp renderizza ogni slide come un'immagine</strong>, non come forme PowerPoint native e modificabili. Ogni slide diventa un'immagine a tutta pagina all'interno dell'involucro <code>.pptx</code>. È perfetto quando devi consegnare un file a qualcuno che vive in PowerPoint e deve solo presentarlo, ma, una volta esportato, non puoi cliccare per modificare una casella di testo, ricolorare una forma o spostare un punto elenco. Se il tuo flusso di lavoro deve continuare a modificare in PowerPoint in seguito, tratta il pptx di Marp come un formato di consegna, non come un formato sorgente. (Modalità di esportazione «modificabile» sperimentali sono andate e venute; il comportamento affidabile e documentato è un'immagine per slide.)</p>
      <h2>Marp vs Slidev vs reveal.js</h2>
      <p>Marp non è l'unico strumento di presentazione Markdown-first. Ecco il confronto onesto lungo gli assi che decidono la tua scelta:</p>
      <table><thead><tr><th>Strumento</th><th>Scrittura</th><th>Esportazione PPTX modificabile</th><th>Templating</th><th>Licenza</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>Markdown puro + direttive</td><td>pptx come immagini (non forme modificabili)</td><td>Temi CSS</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML (plugin Markdown opzionale)</td><td>Nessun pptx nativo</td><td>Temi HTML/CSS</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + componenti Vue</td><td>Nessun pptx nativo (PDF/PNG/SPA)</td><td>Temi Vue + Windi/UnoCSS</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>Descrivilo al tuo agente</td><td>Presentazione modificabile basata su un design system</td><td>Design system + libreria di template</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong> è la scelta quando vuoi il minimo di cerimonie: un unico file Markdown e una CLI. <strong><a href="/blog/reveal-js/">reveal.js</a></strong> è il maturo framework di slide web: HTML-first, personalizzabile all'infinito e con l'esperienza più ricca nel browser, ma scrivi HTML e non c'è un percorso nativo verso PowerPoint. <strong><a href="/blog/slidev/">Slidev</a></strong> sta nel mezzo, per gli sviluppatori che vogliono Markdown più veri componenti Vue, evidenziazione del codice ed embed interattivi: più potenza di Marp, ma anche più configurazione. Tutti e tre hanno licenza MIT e nessuno esporta qualcosa di direttamente modificabile in PowerPoint.</p>
      <h2>Dove si inserisce Open Design</h2>
      <p>Marp eccelle esattamente in una cosa: prendere il Markdown che hai già scritto e trasformarlo in slide. Ma chiede a <em>te</em> di scrivere la presentazione: la struttura, la formulazione, il CSS del tema. Non genera il contenuto, non lo mantiene coerente con il brand tra i progetti, né ti dà un artefatto modificabile una volta esportato.</p>
      <p><a href="/">Open Design</a> è il livello sopra tutto questo. È un <strong>Agent-Native Design Workspace</strong> open source (Apache-2.0), local-first e con le tue chiavi, che si colloca al di fuori dell'agente di codice che già usi. Invece di scrivere Markdown a mano, descrivi la presentazione che vuoi e l'agente ne genera una modificabile basata su un design system, con template di presentazioni HTML di prima parte nella sua <a href="/plugins/templates/">libreria di plugin</a> da cui partire. Non è un sostituto del Marp scritto in Markdown: quando hai già il contenuto in Markdown e vuoi una presentazione veloce dalla riga di comando, Marp è lo strumento giusto. Quando vuoi che la presentazione sia generata, coerente con il brand e ancora modificabile come parte di un flusso di progettazione più ampio, è compito del workspace. Se già crei slide con un agente di codice, guarda <a href="/blog/claude-ppt-skills/">come un agente di codice crea presentazioni</a>.</p>
      <h2>Domande frequenti</h2>
      <p><strong>Cos'è Marp?</strong> Marp è il Markdown Presentation Ecosystem open source del team <code>marp-team</code> su GitHub. Scrivi le slide in Markdown e la Marp CLI le esporta in HTML, PDF o PowerPoint. Ha licenza MIT e include Marp Core, il framework Marpit e un'estensione per VS Code. Non ha alcun legame con il termine ortodontico «MARPE».</p>
      <p><strong>Come esporto una presentazione Marp in PowerPoint?</strong> Installa la Marp CLI (<code>marp-cli</code>) ed esegui <code>marp deck.md --pptx</code>. Nota che il <code>.pptx</code> esportato renderizza ogni slide come un'immagine, non come forme PowerPoint native e modificabili: ottimo per la consegna, ma poi non potrai modificare il testo o le forme dentro PowerPoint.</p>
      <p><strong>Marp può esportare file PowerPoint modificabili?</strong> Non in modo affidabile. Il comportamento PPTX documentato di Marp è un'immagine per slide. Se ti serve un <code>.pptx</code> nativamente modificabile con vere caselle di testo e forme, per quel passaggio Marp è lo strumento sbagliato.</p>
      <p><strong>Marp è gratuito?</strong> Sì. Marp è open source sotto licenza MIT, quindi è libero da usare, auto-ospitare e modificare.</p>
      <p><strong>Marp vs reveal.js vs Slidev — quale dovrei usare?</strong> Scegli Marp per le presentazioni in puro Markdown più semplici e un'esportazione facile in PDF/pptx, reveal.js per un maturo framework di slide web HTML-first con personalizzazione profonda, e Slidev per Markdown più componenti Vue interattivi. Tutti e tre hanno licenza MIT; nessuno esporta PowerPoint direttamente modificabile.</p>
      <h2>In sintesi</h2>
      <p>Marp è il modo più pulito per trasformare in slide il Markdown che hai già: un unico file <code>.md</code>, temi CSS e una CLI che esporta HTML, PDF e PowerPoint. Ricorda solo l'unico limite onesto: il pptx esce come immagini, quindi trattalo come un formato di consegna e non come qualcosa che continuerai a modificare in PowerPoint. Se la tua esigenza è «ho il Markdown, dammi una presentazione», Marp è difficile da battere. Quando la presentazione deve essere generata, restare coerente con il brand e rimanere modificabile all'interno di un flusso di progettazione più ampio, è lì che subentra un <a href="/">Agent-Native Design Workspace</a>.</p>
  tr:
    title: 'Marp: Markdown sunum aracı (ve PowerPoint''e nasıl aktarılır)'
    summary: 'Marp, açık kaynaklı Markdown sunum ekosistemidir; slaytları Markdown ile yazın, ardından Marp CLI ile HTML, PDF veya PowerPoint olarak dışa aktarın. Bu rehber Marp temalarını, Markdown ile yazmayı, marp-cli ile pptx''e (görüntü olarak) aktarmayı, reveal.js ve Slidev ile karşılaştırmayı ve Agent-Native Design Workspace''in devreye girdiği noktayı ele alır.'
    category: 'Araçlar ve beceriler'
    bodyHtml: |
      <p>«marp» diye arattığınızda diş veya ortodonti terimi beklediyseniz, bu onun diğer anlamı: <strong>Marp, Markdown Presentation Ecosystem'dir</strong> — GitHub'daki <code>marp-team</code> ekibinin, düz Markdown yazarak slayt desteleri oluşturmaya yarayan açık kaynaklı (MIT) projesi. Tuval üzerinde kutu sürüklemek yok, özel dosya biçimi yok. <code># Slide title</code> ve birkaç madde yazar, bir iki yönerge ekler, Marp de bunu bir sunuma dönüştürür. (Görmüş olabileceğiniz, alakasız «MARPE», ortodontideki bir üst çene genişletme işlemidir — slaytlarla ilgisi yoktur. Bu makale yalnızca sunum aracı Marp hakkındadır.)</p>
      <p>Marp, açık kaynaklı sunum araçları arasında en basit ve en çok Markdown-yerlisi seçenektir; geliştiricilerin ona yönelmesinin sebebi tam da budur. Bu, Marp'ın ne olduğu, yazım ve temaların nasıl çalıştığı, Marp CLI'nin PowerPoint ve PDF'e nasıl aktardığı (ve insanları hazırlıksız yakalayan tek uyarı), reveal.js ile Slidev karşısında nasıl durduğu ve Markdown yolun sonuna geldiğinde bir design workspace'in nerede devreye girdiği konusunda dürüst bir rehberdir.</p>
      <h2>Marp nedir</h2>
      <p>Marp, tek bir fikir etrafında kurulmuş küçük bir araç ekosistemidir: <strong>Markdown olarak slaytlar</strong>. Üç hareketli parçası vardır. <strong>Marpit</strong>, Markdown ile bir temayı slayt HTML ve CSS'ine dönüştüren alt seviye çerçevedir. <strong>Marp Core</strong>, Marpit'in üzerine oturan, varsayılan tema, görsel işleme ve matematik desteğiyle gelen kullanıma hazır motordur. <strong>Marp CLI</strong> (<code>marp-team/marp-cli</code>), Markdown dosyanızı HTML, PDF veya PowerPoint'e dönüştüren komut satırı aracıdır. Ayrıca siz yazarken canlı yan yana önizleme sunan resmi bir <strong>VS Code eklentisi</strong> de vardır.</p>
      <p>Yazımı tek bir <code>.md</code> dosyasında yaparsınız. Slaytlar <code>---</code> ile ayrılır, slayt başına ve genel ayarlar Marp yönergeleriyle (<code>theme:</code>, <code>paginate:</code> veya <code>class:</code> gibi) belirlenir ve her şey sürüm denetimine uygun düz metin olarak kalır. Bütün cazibesi de burada: desteniz Git'te temiz biçimde diff'lenir, kodunuzun yanında yaşar ve sizi asla ikili bir biçime hapsetmez.</p>
      <h2>Marp temaları ve Markdown ile yazım</h2>
      <p>Bir Marp destesi yazmak bilinçli olarak sadedir. Dosyanın en üstündeki bir front-matter bloğu genel yönergeleri belirler, <code>---</code> slaytları böler ve standart Markdown — başlıklar, listeler, kod blokları, görseller, tablolar — içeriğiniz olur. Marp arka planda HTML ve CSS'e dönüştürdüğü için, Markdown'ın ifade edebildiği her şey öğrenilecek ekstra bir söz dizimi olmadan bir slayta eşlenir.</p>
      <p><strong>Marp temaları</strong> yalnızca CSS'tir. Marp Core birkaç yerleşik tema (<code>default</code>, <code>gaia</code>, <code>uncover</code>) ile gelir ve aralarında tek bir <code>theme:</code> yönergesiyle geçiş yaparsınız. Yerleşikler yetmediğinde kendi temanızı bir CSS dosyası olarak yazarsınız — Marpit'in açığa çıkardığı sınıfları hedefleyin, <code>--theme</code> veya <code>--theme-set</code> ile CLI'ye kaydedin, her slayt onu benimser. Satır içi ince ayarlar için tek bir slayta kapsamı sınırlı bir <code>&lt;style&gt;</code> bloğu bırakabilirsiniz. Bu CSS-yerlisi model, Marp'ın hem tavanı hem tabanıdır: CSS'te rahatsanız tam denetime sahip olursunuz, değilseniz varsayılanlar yine de temiz görünür.</p>
      <h2>Marp'ı PowerPoint ve PDF'e aktarma</h2>
      <p>Marp CLI asıl değerini dışa aktarımda kanıtlar. Onu Markdown'ınıza yönlendirin ve bir hedef seçin:</p>
      <ul><li><strong>HTML</strong> — <code>marp deck.md -o deck.html</code>, tarayıcıda sunduğunuz kendi kendine yeten bir web destesi üretir.</li><li><strong>PDF</strong> — <code>marp deck.md --pdf</code>, baskıya hazır bir PDF oluşturur; broşürler ve paylaşım için idealdir.</li><li><strong>PowerPoint</strong> — <code>marp deck.md --pptx</code>, PowerPoint veya Keynote'ta açabileceğiniz bir <code>.pptx</code> dosyası dışa aktarır.</li></ul>
      <p>Açıkça söylenmeye değer tek uyarı: <strong>Marp'ın PPTX dışa aktarımı her slaytı bir görsel olarak işler</strong>, yerel ve düzenlenebilir PowerPoint şekilleri olarak değil. Her slayt, <code>.pptx</code> sarmalayıcısının içinde tam kenarlıklı bir resme dönüşür. PowerPoint'ten çıkmayan ve dosyayı yalnızca sunması gereken birine dosya teslim etmeniz gerektiğinde bu mükemmeldir — ama dışa aktardıktan sonra bir metin kutusuna tıklayıp düzenleyemez, bir şeklin rengini değiştiremez veya bir maddeyi kaydıramazsınız. İş akışınızın sonradan PowerPoint'te düzenlemeye devam etmesi gerekiyorsa, Marp'ın pptx'ini bir kaynak biçimi değil, bir teslim biçimi olarak görün. (Deneysel «düzenlenebilir» dışa aktarım modları gelip gitti; güvenilir, belgelenmiş davranış slayt başına bir görseldir.)</p>
      <h2>Marp vs Slidev vs reveal.js</h2>
      <p>Marp, Markdown öncelikli tek sunum aracı değildir. Seçiminizi belirleyen eksenler boyunca dürüst karşılaştırma şöyle:</p>
      <table><thead><tr><th>Araç</th><th>Yazım</th><th>Düzenlenebilir PPTX dışa aktarımı</th><th>Şablonlama</th><th>Lisans</th></tr></thead><tbody><tr><td><strong>Marp</strong></td><td>Saf Markdown + yönergeler</td><td>pptx görsel olarak (düzenlenebilir şekil değil)</td><td>CSS temaları</td><td>MIT</td></tr><tr><td><strong>reveal.js</strong></td><td>HTML (Markdown eklentisi isteğe bağlı)</td><td>Yerel pptx yok</td><td>HTML/CSS temaları</td><td>MIT</td></tr><tr><td><strong>Slidev</strong></td><td>Markdown + Vue bileşenleri</td><td>Yerel pptx yok (PDF/PNG/SPA)</td><td>Vue + Windi/UnoCSS temaları</td><td>MIT</td></tr><tr><td><strong>Open Design</strong></td><td>Ajanınıza tarif edin</td><td>Bir tasarım sistemine göre düzenlenebilir deste</td><td>Tasarım sistemi + şablon kütüphanesi</td><td>Apache-2.0</td></tr></tbody></table>
      <p><strong>Marp</strong>, en az tören istediğinizde tercihtir — tek bir Markdown dosyası ve bir CLI. <strong><a href="/blog/reveal-js/">reveal.js</a></strong>, olgun web slayt çerçevesidir: HTML öncelikli, sonsuzca özelleştirilebilir ve tarayıcı içinde en zengin deneyimi sunar, ama HTML yazarsınız ve yerel bir PowerPoint yolu yoktur. <strong><a href="/blog/slidev/">Slidev</a></strong> ortada durur; Markdown'a ek olarak gerçek Vue bileşenleri, kod vurgulama ve etkileşimli gömmeler isteyen geliştiriciler içindir — Marp'tan daha güçlü, ama kurulumu da daha fazla. Üçü de MIT lisanslıdır ve hiçbiri PowerPoint'te doğrudan düzenlenebilir bir şey dışa aktarmaz.</p>
      <h2>Open Design nereye oturur</h2>
      <p>Marp tam olarak tek bir şeyde mükemmeldir: zaten yazdığınız Markdown'ı alıp slaytlara dönüştürmek. Ama desteyi yazmayı <em>sizden</em> ister — yapıyı, ifadeyi, tema CSS'ini. İçeriği üretmez, projeler arasında marka çizgisinde tutmaz ve dışa aktardıktan sonra size düzenlenebilir bir çıktı vermez.</p>
      <p><a href="/">Open Design</a>, bunun bir üst katmanıdır. Zaten kullandığınız kodlama ajanının dışında konumlanan, açık kaynaklı (Apache-2.0), yerel öncelikli, kendi anahtarını getir yaklaşımlı bir <strong>Agent-Native Design Workspace</strong>'tir. Markdown'ı elle yazmak yerine istediğiniz desteyi tarif edersiniz ve ajan, bir tasarım sistemine göre düzenlenebilir bir tane üretir — başlangıç için <a href="/plugins/templates/">eklenti kütüphanesinde</a> birinci taraf HTML deste şablonları vardır. Markdown ile yazılan Marp'ın yerini almaz: içerik zaten Markdown'daysa ve komut satırından hızlı bir deste istiyorsanız doğru araç Marp'tır. Destenin üretilmesini, marka çizgisinde kalmasını ve daha büyük bir tasarım akışının parçası olarak düzenlenebilir kalmasını istiyorsanız, bu workspace'in işidir. Zaten bir kodlama ajanıyla slayt yapıyorsanız, <a href="/blog/claude-ppt-skills/">bir kodlama ajanının slayt destelerini nasıl yaptığına</a> bakın.</p>
      <h2>Sıkça sorulan sorular</h2>
      <p><strong>Marp nedir?</strong> Marp, GitHub'daki <code>marp-team</code> ekibinin açık kaynaklı Markdown Presentation Ecosystem'idir. Slaytları Markdown ile yazarsınız ve Marp CLI bunları HTML, PDF veya PowerPoint'e aktarır. MIT lisanslıdır ve Marp Core'u, Marpit çerçevesini ve bir VS Code eklentisini içerir. Ortodontideki «MARPE» terimiyle ilgisi yoktur.</p>
      <p><strong>Bir Marp destesini PowerPoint'e nasıl aktarırım?</strong> Marp CLI'yi (<code>marp-cli</code>) kurun ve <code>marp deck.md --pptx</code> komutunu çalıştırın. Dışa aktarılan <code>.pptx</code>'in her slaytı yerel düzenlenebilir PowerPoint şekilleri olarak değil, bir görsel olarak işlediğini unutmayın — devir teslim için harikadır, ama sonradan PowerPoint içinde metni veya şekilleri düzenleyemezsiniz.</p>
      <p><strong>Marp düzenlenebilir PowerPoint dosyaları dışa aktarabilir mi?</strong> Güvenilir biçimde hayır. Marp'ın belgelenmiş PPTX davranışı slayt başına bir görseldir. Gerçek metin kutuları ve şekilleri olan, yerel olarak düzenlenebilir bir <code>.pptx</code>'e ihtiyacınız varsa, o adım için Marp yanlış araçtır.</p>
      <p><strong>Marp ücretsiz mi?</strong> Evet. Marp, MIT lisansı altında açık kaynaktır; dolayısıyla özgürce kullanılabilir, kendiniz barındırabilir ve değiştirebilirsiniz.</p>
      <p><strong>Marp vs reveal.js vs Slidev — hangisini kullanmalıyım?</strong> En basit saf Markdown desteleri ve kolay PDF/pptx dışa aktarımı için Marp'ı, derin özelleştirme sunan olgun, HTML öncelikli bir web slayt çerçevesi için reveal.js'i ve Markdown'a ek etkileşimli Vue bileşenleri için Slidev'i seçin. Üçü de MIT lisanslıdır; hiçbiri doğrudan düzenlenebilir PowerPoint dışa aktarmaz.</p>
      <h2>Özetle</h2>
      <p>Marp, elinizde zaten olan Markdown'ı slaytlara dönüştürmenin en temiz yoludur — tek bir <code>.md</code> dosyası, CSS temaları ve HTML, PDF ile PowerPoint'e aktaran bir CLI. Yalnızca tek dürüst sınırlamayı hatırlayın: pptx görsel olarak çıkar, bu yüzden onu PowerPoint'te düzenlemeye devam edeceğiniz bir şey değil, bir teslim biçimi olarak görün. İhtiyacınız «Markdown bende, bana bir deste ver» ise, Marp'ı geçmek zordur. Deste üretilmeli, marka çizgisinde kalmalı ve daha büyük bir tasarım akışı içinde düzenlenebilir olmalıysa, işte orada bir <a href="/">Agent-Native Design Workspace</a> devreye girer.</p>
---

If you searched for "marp" expecting a dental or orthodontic term, this is the other one: **Marp is the Markdown Presentation Ecosystem** — an open-source (MIT) project from the `marp-team` on GitHub for building slide decks by writing plain Markdown. No dragging boxes on a canvas, no proprietary file format. You write `# Slide title` and a few bullets, add a directive or two, and Marp turns it into a presentation. (The unrelated "MARPE" you may have seen is a maxillary expansion procedure in orthodontics — nothing to do with slides. This article is strictly about Marp, the presentation tool.)

Marp is the simplest, most Markdown-native option among the open-source presentation tools, which is exactly why developers reach for it. This is an honest guide to what Marp is, how authoring and themes work, how the Marp CLI exports to PowerPoint and PDF (and the one caveat that catches people out), how it stacks up against reveal.js and Slidev, and where a design workspace picks up when Markdown runs out of road.

## What Marp is

Marp is a small ecosystem of tools built around one idea: **slides as Markdown**. It has three moving parts. **Marpit** is the low-level framework that turns Markdown plus a theme into slide HTML and CSS. **Marp Core** is the batteries-included engine on top of Marpit, with a default theme, image handling, and math support. **Marp CLI** (`marp-team/marp-cli`) is the command-line tool that renders your Markdown file into HTML, PDF, or PowerPoint. There's also an official **VS Code extension** that gives you a live side-by-side preview as you type.

You author in one `.md` file. Slides are separated by `---`, per-slide and global settings are set through Marp directives (like `theme:`, `paginate:`, or `class:`), and everything stays in version-controllable plain text. That's the whole appeal: your deck diffs cleanly in Git, lives next to your code, and never locks you into a binary format.

## Marp themes and Markdown authoring

Authoring a Marp deck is deliberately minimal. A front-matter block at the top of the file sets global directives, `---` breaks slides, and standard Markdown — headings, lists, code fences, images, tables — becomes your content. Because Marp renders to HTML and CSS under the hood, anything Markdown expresses maps to a slide without extra syntax to learn.

**Marp themes** are just CSS. Marp Core ships a handful of built-in themes (`default`, `gaia`, `uncover`), and you switch between them with a single `theme:` directive. When the built-ins aren't enough, you write your own theme as a CSS file — target the classes Marpit exposes, register it with the CLI via `--theme` or `--theme-set`, and every slide picks it up. For inline tweaks you can drop a scoped `<style>` block into a single slide. This CSS-native model is Marp's real ceiling and floor: if you're comfortable with CSS you have full control, and if you're not, the defaults still look clean.

## Exporting Marp to PowerPoint and PDF

Export is where the Marp CLI earns its place. Point it at your Markdown and choose a target:

- **HTML** — `marp deck.md -o deck.html` produces a self-contained web deck you present in the browser.
- **PDF** — `marp deck.md --pdf` renders a print-ready PDF, ideal for handouts and sharing.
- **PowerPoint** — `marp deck.md --pptx` exports a `.pptx` file you can open in PowerPoint or Keynote.

The one caveat worth stating plainly: **Marp's PPTX export renders each slide as an image**, not as native, editable PowerPoint shapes. Every slide becomes a full-bleed picture inside the `.pptx` wrapper. That's perfect when you need to hand a file to someone who lives in PowerPoint and only has to present it — but you can't click in and edit a text box, recolor a shape, or nudge a bullet once it's exported. If your workflow needs to keep editing in PowerPoint afterward, treat Marp's pptx as a delivery format, not a source format. (Experimental "editable" export modes have come and gone; the reliable, documented behavior is image-per-slide.)

## Marp vs Slidev vs reveal.js

Marp isn't the only Markdown-first presentation tool. Here's the honest comparison across the axes that decide your pick:

| Tool | Authoring | Editable PPTX export | Templating | License |
| --- | --- | --- | --- | --- |
| **Marp** | Pure Markdown + directives | pptx as images (not editable shapes) | CSS themes | MIT |
| **reveal.js** | HTML (Markdown plugin optional) | No native pptx | HTML/CSS themes | MIT |
| **Slidev** | Markdown + Vue components | No native pptx (PDF/PNG/SPA) | Vue + Windi/UnoCSS themes | MIT |
| **Open Design** | Describe it to your agent | Editable deck against a design system | Design system + template library | Apache-2.0 |

**Marp** is the choice when you want the least ceremony — a single Markdown file and a CLI. **[reveal.js](/blog/reveal-js/)** is the mature web-slides framework: HTML-first, endlessly customizable, and the richest in-browser experience, but you're writing HTML and there's no native PowerPoint path. **[Slidev](/blog/slidev/)** sits in the middle for developers who want Markdown plus real Vue components, code highlighting, and interactive embeds — more power than Marp, more setup too. All three are MIT-licensed and export nothing directly editable in PowerPoint.

## Where Open Design fits

Marp is excellent at exactly one thing: taking Markdown you've already written and turning it into slides. But it asks *you* to write the deck — the structure, the wording, the theme CSS. It doesn't generate the content, keep it on-brand across projects, or give you an editable artifact once it's exported.

[Open Design](/) is the layer above that. It's an open-source (Apache-2.0), local-first, bring-your-own-key **Agent-Native Design Workspace** that sits outside the coding agent you already use. Instead of hand-writing Markdown, you describe the deck you want, and the agent generates an editable one against a design system — with first-party HTML deck templates in its [plugin library](/plugins/templates/) to start from. It's not a replacement for Markdown-authored Marp: when you already have the content in Markdown and want a quick deck from the command line, Marp is the right tool. When you want the deck generated, on-brand, and still editable as part of a larger design workflow, that's the workspace's job. If you already make slides with a coding agent, see [how a coding agent makes slide decks](/blog/claude-ppt-skills/).

## FAQ

**What is Marp?** Marp is the open-source Markdown Presentation Ecosystem from the `marp-team` on GitHub. You write slides in Markdown, and the Marp CLI exports them to HTML, PDF, or PowerPoint. It's MIT-licensed and includes Marp Core, the Marpit framework, and a VS Code extension. It is not related to the orthodontic "MARPE" term.

**How do I export a Marp deck to PowerPoint?** Install the Marp CLI (`marp-cli`) and run `marp deck.md --pptx`. Note that the exported `.pptx` renders each slide as an image, not as native editable PowerPoint shapes — it's great for handoff, but you can't edit the text or shapes inside PowerPoint afterward.

**Can Marp export editable PowerPoint files?** Not reliably. Marp's documented PPTX behavior is one image per slide. If you need natively editable `.pptx` with real text boxes and shapes, Marp is the wrong tool for that step.

**Is Marp free?** Yes. Marp is open source under the MIT license, so it's free to use, self-host, and modify.

**Marp vs reveal.js vs Slidev — which should I use?** Choose Marp for the simplest pure-Markdown decks and easy PDF/pptx export, reveal.js for a mature HTML-first web-slides framework with deep customization, and Slidev for Markdown plus interactive Vue components. All three are MIT-licensed; none export directly editable PowerPoint.

## The takeaway

Marp is the cleanest way to turn Markdown you've already got into slides — a single `.md` file, CSS themes, and a CLI that exports HTML, PDF, and PowerPoint. Just remember the one honest limitation: the pptx comes out as images, so treat it as a delivery format rather than something you'll keep editing in PowerPoint. If your need is "I have the Markdown, give me a deck," Marp is hard to beat. When the deck needs to be generated, stay on-brand, and remain editable inside a bigger design workflow, that's where an [Agent-Native Design Workspace](/) picks up.
