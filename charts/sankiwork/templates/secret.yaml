apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sankiwork.fullname" . }}
type: Opaque
data:
  {{- if .Values.config.apiToken }}
  SW_API_TOKEN: {{ .Values.config.apiToken | b64enc | quote }}
  {{- else }}
  {{- fail "CRITICAL ERROR: config.apiToken cannot be empty." }}
  {{- end }}