trigger: none

parameters:
  - name: azureServiceConnection
    type: string
    default: '<your-azure-service-connection>'

variables:
  vmImageName: ubuntu-latest
  resourceGroupName: sankiwork-aci
  deploymentName: sankiwork-aci
  location: eastus
  templateFile: deploy/azure/container-instance.bicep
  sankiWorkImage: ghcr.io/nexu-io/od:latest
  dnsNameLabel: sankiwork-$(Build.BuildId)
  browserOrigin: https://od.example.com

pool:
  vmImage: $(vmImageName)

steps:
  - checkout: self

  - task: AzureCLI@2
    displayName: Deploy SankiWork to Azure Container Instances
    inputs:
      azureSubscription: ${{ parameters.azureServiceConnection }}
      scriptType: bash
      scriptLocation: inlineScript
      useGlobalConfig: false
      inlineScript: |
        set -euo pipefail

        if [ -z "${SW_API_TOKEN:-}" ]; then
          echo "Set SW_API_TOKEN as a secret pipeline variable before running this pipeline." >&2
          exit 1
        fi

        if [ "$(browserOrigin)" = "https://od.example.com" ]; then
          echo "Set browserOrigin to the TLS origin served by your authenticated reverse proxy." >&2
          exit 1
        fi

        az group create \
          --name "$(resourceGroupName)" \
          --location "$(location)"

        az deployment group create \
          --name "$(deploymentName)" \
          --resource-group "$(resourceGroupName)" \
          --template-file "$(templateFile)" \
          --parameters \
            odApiToken="$SW_API_TOKEN" \
            image="$(sankiWorkImage)" \
            dnsNameLabel="$(dnsNameLabel)" \
            allowedOrigins="$(browserOrigin)"
    env:
      SW_API_TOKEN: $(SW_API_TOKEN)
