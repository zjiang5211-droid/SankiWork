# Prototype Dashboard

This is the full reference example for a spec create plugin. It includes a portable `SKILL.md`, OD sidecar, preview, seed asset, and eval file.

```bash
sw plugin validate .
sw plugin install .
sw plugin apply create-prototype-dashboard --input audience="support leads" --input topic="enterprise onboarding risk"
```

